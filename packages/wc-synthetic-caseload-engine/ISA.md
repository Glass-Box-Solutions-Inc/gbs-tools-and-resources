---
task: "Seed-driven WC synthetic attorney case-file engine"
project: wc-synthetic-caseload-engine
slug: 20260727-143600_wc-synthetic-caseload-engine
effort: E4
effort_source: classifier
phase: execute
progress: 247/254
iteration: 17
mode: interactive
started: 2026-07-27T14:36:00-07:00
updated: 2026-08-14T21:37:32-07:00
ticket: AJC-34
---

# ISA — WC Synthetic Caseload Engine

## Problem

Adjudica demo accounts hold five document-free matters and one hand-populated case (Garcia). There is no repeatable way to produce complete, realistic, synthetic CA workers' compensation attorney case files on demand. The closest tool, `merus-test-data-generator`, generates documents at scale but takes only CLI flags (no per-case seed artifact), models no reconsideration round-trips, models lien resolution thinly, is 3 subtypes stale against the classifier (350 vs 353), and offers no fine-grained per-case document type/count controls. The classifier's accuracy corpus covers only 97 of 353 subtypes for the same reason: nothing can generate targeted documents per specification.

## Vision

Alex writes (or auto-derives) a small YAML seed per case — who was hurt, how, how far the case went, which doctrines flavor it, exactly which documents in what quantities and formats — runs one command, and gets back complete attorney case files that read like a real applicant-side firm produced them: through lien conferences and executed lien agreements, through a petition for reconsideration that came back remanded and settled on remand. The same seeds regenerate byte-similar caseloads forever; the demo accounts stop being empty shells.

## Out of Scope

- Modifying `merus-test-data-generator` in place (Alex: "this becomes a new tool in that same repo"); it is consumed as a library.
- Adjudica-side ingestion code (generalizing `copy-garcia-to-prod.ts` to N matters lives in adjudica-ai-app; this engine only emits a compatible manifest).
- MerusCase push execution in v1 (reuse of the existing orchestration is wired as an optional export target, but Browserless case-creation runs are a separate operational task).
- Pixel-accurate official DWC form overlays (ReportLab approximations inherited from the substrate; form-overlay work remains unstarted upstream).
- Non-California jurisdictions. (Defense-POV caseloads were originally out of scope; Alex pulled them IN scope 2026-07-27 evening — see ISC-75..84.)
- Frontend UI; live LLM-generated prose in v1 (deterministic template substrate first; KB grounding hooks are structured, not free-generated).

## Principles

- The seed IS the interface: everything a case needs must be expressible — and is surfaced — in one reviewable artifact per case.
- Determinism before flourish: same seed + same version → same caseload; realism features (scan noise, messiness) must be seed-derived, never wall-clock-derived.
- The classifier taxonomy is the vocabulary of record: generate what the classifier can name, validate round-trip against it.
- Reuse the proven substrate: templates, content pools, and lifecycle machinery come from merus-test-data-generator; this engine adds control, not duplication.
- Synthetic data only — no real PHI/PII ever; `zeroRealPii: true` must be honestly assertable on every manifest.

## Constraints

- New standalone package `packages/wc-synthetic-caseload-engine/` following repo conventions (README/CLAUDE/CHANGELOG/PLANS_APPROVED/PROGRAMMING_PRACTICES quartet, GBS footer, registry entries in root README/CLAUDE/ci.yml).
- Python 3.12, `pyproject.toml` + `src/` layout + console script (the insurance-claims-case-generator packaging pattern) — Python is inherited from the substrate whose templates we must reuse; flagged to Alex as an explicit stack decision.
- merus-test-data-generator consumed via documented `sys.path` bridge (the repo's existing cross-package pattern); no file copying of its modules.
- Taxonomy keys must match `Adjudica-classifier` v2 (353 subtypes) exactly; umbrella subtypes emitted only when explicitly seeded.
- KB access is network-optional: generation must succeed fully offline; grounding enrichment degrades gracefully.
- Linear ticket AJC-34 (moved from ADJ-1818, see Decisions); conventional commits scoped `feat(wc-synthetic-caseload-engine)`.

## Goal

A new installable package whose CLI turns a caseload spec (defaults + distributions + per-case seeds) into complete synthetic CA WC attorney case files — with per-case seeds surfaced as reviewable YAML, fine-grained document type/count/format controls, lifecycles that reach lien resolution with lien agreements and reconsideration round-trips with post-recon litigation or settlement — validated by taxonomy round-trip and demonstrated by a generated example caseload committed as evidence.

## Criteria

### Package & CLI
- [x] ISC-1: `packages/wc-synthetic-caseload-engine/pyproject.toml` exists with `src/` layout and console script `wc-caseload`
- [x] ISC-2: `pip install -e .` (or `uv pip install -e .`) succeeds in a 3.12 venv
- [x] ISC-3: `wc-caseload --help` lists `generate`, `seed`, `validate`, `taxonomy-check` commands
- [x] ISC-4: `wc-caseload seed --template` writes an annotated example CaseSeed YAML covering every controllable field
- [x] ISC-5: `wc-caseload generate --spec <yaml> --out <dir>` runs end-to-end without network access
- [x] ISC-6: README.md documents seed schema, controls, lifecycle paths, and all CLI commands
- [x] ISC-7: CLAUDE.md, CHANGELOG.md, PLANS_APPROVED.md, PROGRAMMING_PRACTICES.md, .planning/STATE.md exist per repo convention
- [x] ISC-8: All package markdown files end with the GBS footer
- [x] ISC-9: Root README.md package table + root CLAUDE.md registry include the new package
- [x] ISC-10: `.github/workflows/ci.yml` has a paths-filter entry and quality-gate job for the package

### Seed schema (the surfaced "needed seed")
- [x] ISC-11: Pydantic `CaseSeed` model exists with profile, injury, lifecycle, documents, output sections
- [x] ISC-12: `CaseSeed.rng_seed` fully determines generation: two runs with the same seed produce identical manifests (hash-compared), across processes, across timezones, and **across any inherited `PYTHONHASHSEED`** — only `"0"` is accepted as stable, so `1`, `2` and `random` all re-exec instead of being trusted as deliberate, and **no pre-set environment variable can waive that check**: the re-exec sentinel is a bounded hop counter, never a certificate of stability (subprocess regressions compare whole trees under `=1` vs `=2`, both with and without `WC_CASELOAD_HASH_PINNED` pre-set)
- [x] ISC-13: Injury section supports `specific | cumulative_trauma | death` with 1–5 body parts each carrying ICD-10
- [x] ISC-14: Lifecycle section supports `claim_response: accepted | delayed | denied`
- [x] ISC-15: Lifecycle section supports `eval_type: qme | ame | none`
- [x] ISC-16: Resolution supports `stipulations | c_and_r | findings_award | take_nothing | pending`
- [x] ISC-17: `CaseloadSpec` model supports defaults + explicit `cases[]` + `auto: {count, distribution}` derivation
- [x] ISC-18: Auto-derived cases write their resolved CaseSeed YAML to the output dir (the seed is always surfaced per case)
- [x] ISC-19: Seed loader rejects unknown fields and invalid enum values with actionable errors (probe: bad seed → nonzero exit + named field)
- [x] ISC-20: Every distribution preset from the KB PRD (§3) is available (`balanced`, `early_stage`, `settlement_heavy`, `complex_litigation` at minimum)
- [x] ISC-21: Doctrine hooks field accepts the 12 KB PRD §6 landmark hooks and injects matching content flags into the document plan
- [x] ISC-21.1: `doctrine.py` content table covers all 14 `DoctrineHook` enum values — each with a controlling citation marker, ≥1 medical or legal paragraph pool, and ≥1 target subtype (probe: table-completeness pytest)
- [x] ISC-21.2: Every target subtype in the doctrine table is a canonical classifier taxonomy key (probe: pytest asserting membership in the 353-key set)
- [x] ISC-21.3: `PlannedDocument.content_flags` carries the matching hooks on planned documents whose subtype a seeded hook targets (probe: `build_case_plan` pytest per hook)
- [x] ISC-21.4: For each of the 14 hooks, a rendered flagged document's extracted text contains that hook's citation marker (probe: render + pypdf text grep, one seed per hook)
- [x] ISC-21.5: Anti: a seed with no doctrine hooks renders documents through the unmodified template path with zero doctrine citations or injected headings in extracted text (probe: negative-grep pytest + code-path assertion; keys on citations/headings, not bare markers — see Changelog)
- [x] ISC-21.6: Per-document manifest entries surface `contentFlags` only when non-empty, and doctrine injection is seed-deterministic (probe: manifest pytest + double-render md5 compare of a flagged case)

### Fine-grained document controls
- [x] ISC-22: `documents.include_only` whitelist restricts emission to named subtypes/types
- [x] ISC-23: `documents.exclude` blacklist suppresses named subtypes/types
- [x] ISC-24: Per-subtype count override (`{subtype, count}`) emits exactly that count
- [x] ISC-25: Per-parent-type min/max override bounds emission for that type
- [x] ISC-26: `documents.global_cap` bounds total documents per case
- [x] ISC-27: `documents.format_mix` weights pdf/scanned_pdf/eml/docx assignment and is honored within tolerance (probe: 100-doc case, chi-square sanity)
- [x] ISC-28: Controls compose: whitelist + override + cap resolve deterministically with documented precedence
- [x] ISC-29: A control demanding a subtype invalid for the case's lifecycle emits it anyway with a WARN (explicit control wins, loudly)

### Lifecycle — core
- [x] ISC-30: Lifecycle walk reaches every `target_stage` value (probe: one seed per stage, stage recorded in manifest)
- [x] ISC-31: Denied-claim path emits `CLAIM_DENIAL_LETTER` and applicant-side response documents
- [x] ISC-32: UR dispute path emits RFA → UR decision → written denial/authorization → (optional) IMR application/determination in **strictly increasing** date order, including on a seed whose injury sits exactly on the `ur_dispute` / `imr` runway floor (the chain is fitted, not clamped)
- [x] ISC-33: Medical-legal path emits QME panel forms and QME/AME reports consistent with `eval_type`, with panel request → panel issuance → report in **strictly increasing** date order, including on a seed sitting exactly on the 240-day eval runway floor
- [x] ISC-34: C&R resolution emits `COMPROMISE_AND_RELEASE_*` + `ORDER_APPROVING_SETTLEMENT`
- [x] ISC-35: Stips resolution emits `STIPULATIONS_WITH_REQUEST_FOR_AWARD_*` + award order
- [x] ISC-36: Trial resolution emits `MINUTES_OF_HEARING`/`FINDINGS_AND_AWARD` + `OPINION_ON_DECISION`
- [x] ISC-37: Every emitted document's date is consistent with the lifecycle ordering (probe: manifest dates monotone within statutory windows; **every sequenced chain — denial response, lien tracks, recon round trip — is fitted via `fit_track` rather than clamped per document**, and each branch that consumes calendar has a runway floor in `seeds.runway_demands()` so a seed too short for its own chain is rejected at load instead of collapsing onto the anchor)
- [x] ISC-38: Document dates respect key statutory windows encoded in the substrate (90-day denial, QME timelines) — spot-probe two windows

### Lifecycle — liens through resolution (new capability)
- [x] ISC-39: `liens.count` N > 0 creates N distinct lien claimant tracks with claimant-type from `liens.claimants`
- [x] ISC-40: Each lien track emits `NOTICE_OF_LIEN_FILING` (or `LIEN_*` claim subtype matching claimant type)
- [x] ISC-41: Lien conference path emits `NOTICE_OF_LIEN_CONFERENCE` and `PRETRIAL_CONFERENCE_STATEMENT_LIEN` when seeded
- [x] ISC-42: Lien resolution `lien_resolution_agreement` emits `LIEN_RESOLUTION` ("Lien Resolution Agreement") — the explicitly requested artifact
- [x] ISC-43: Lien resolution `lien_stipulation` emits `LIEN_STIPULATION_AGREEMENT`
- [x] ISC-44: Lien resolutions `dismissal` and `order_on_lien` emit `LIEN_DISMISSAL` / `ORDER_ON_LIEN` respectively
- [x] ISC-45: Lien documents post-date case-in-chief resolution when seeded as post-resolution lien litigation (the common real-world shape)
- [x] ISC-46: EDD lien claimant type emits `LIEN_EDD_OVERPAYMENT`

### Lifecycle — reconsideration round-trip (new capability)
- [x] ISC-47: `reconsideration.enabled` after a trial/award emits `PETITION_RECONSIDERATION_FILED`
- [x] ISC-48: Recon track emits `PETITION_RECONSIDERATION_OPPOSITION` and (probabilistic, seedable) `PETITION_RECONSIDERATION_REPLY`, **strictly ordered petition < opposition < reply < order** — the chain is built in legal sequence and the dates fitted to it, never sorted into whatever order independent draws produced (property test over 50 seeds)
- [x] ISC-49: Recon track emits `ORDER_ON_RECONSIDERATION` with outcome from seed (`denied | granted_remand | granted_reversed`)
- [x] ISC-50: `granted_remand` + `post_recon: further_litigation` emits post-recon litigation documents (new DOR, hearing minutes, amended F&A)
- [x] ISC-51: `granted_remand` + `post_recon: settled` emits a post-recon C&R or Stips with approval order dated after the recon order
- [x] ISC-52: `denied` + `post_recon: affirmed_final` closes the case with no post-recon litigation documents
- [x] ISC-53: Recon document dates honor the 20/25-day petition window and sequence (petition ≤ 25 days after F&A service; probe manifest dates). The LC 5909 sixty-day order window is honoured **except** where the briefing schedule outruns it: the ruling is floored at the day after the last brief, because an order a few days late is an ordinary file and an order predating the briefing is not one at all (counted, ≤10 in 50 seeds)

### Rendering & realism
- [x] ISC-54: Every canonical subtype renders without fallback — all-353 forced-render test drives each subtype through this engine's dispatch path and asserts zero `GenericDocumentTemplate` resolutions, zero `RenderResult.fallback`, and non-trivial output (>500 B, PDF parses with ≥1 page)
- [x] ISC-55: All four output formats are produced when seeded (pdf, scanned_pdf, eml, docx) — probe file magic bytes
- [x] ISC-56: Every generated PDF exceeds 500 bytes and opens (pymupdf page count ≥ 1)
- [x] ISC-57: Scan-simulation seeding is derived from `rng_seed` + doc index, not `hash()`/wall clock (fixes the substrate's PYTHONHASHSEED leak within this engine's calls)
- [x] ISC-58: Case-level narrative coherence: applicant name, ADJ number, DOI, employer, carrier identical across all documents in a case (probe: grep manifest fields against sampled rendered text)
- [x] ISC-59: Party block includes applicant attorney + firm (applicant-side POV) on pleadings and correspondence

### Taxonomy & classifier integration
- [x] ISC-60: Engine taxonomy import matches Adjudica-classifier at exactly 353 subtypes (probe: count + set-diff test = ∅)
- [x] ISC-61: `wc-caseload taxonomy-check` compares against the classifier source tree and exits nonzero on drift
- [x] ISC-62: `wc-caseload validate --out <dir>` verifies every manifest subtype is a valid classifier key with valid parent mapping, and fails on any `fallback: true` document unless `--allow-fallback` is passed
- [x] ISC-63: Optional corpus filename mode emits `TC-###_###_<SUBTYPE>_<YYYY-MM-DD>.pdf` names accepted by the classifier's sampling regex
- [x] ISC-64: Default filename mode is neutral (no subtype leak) for honest classification measurement

### Outputs & manifests
- [x] ISC-65: Per-case output folder contains rendered documents + `seed.yaml` + `manifest.json`
- [x] ISC-66: `manifest.json` carries per-document `{filename, subtype, type, format, documentDate, md5Checksum, fileSize, mimeType, template, fallback}`
- [x] ISC-67: Manifest carries a `provenance` block with a computed `zeroRealPii`, its `castProvenance` derivation, and generator version + seed hash
- [x] ISC-68: Caseload-level `caseload_manifest.json` aggregates cases with stage/resolution/lien/recon summary fields
- [x] ISC-69: Example caseload spec in `examples/` regenerates deterministically (committed spec, verified hashes)

### Quality gates
- [x] ISC-70: `pytest` suite passes with ≥ 25 tests covering seeds, controls, lifecycle paths, determinism
- [x] ISC-71: `ruff check` passes clean on the package
- [x] ISC-72: Anti: no module from merus-test-data-generator is copied into this package (probe: no duplicated file contents; bridge imports only)
- [x] ISC-73: Anti: no real names/PHI patterns in generated output (probe: **two** sweeps — the curated `src/wc_caseload_engine/data/name_denylist.txt` against extracted text of every text-bearing demo document **and** every manifest cast field, plus a dynamic sweep of **all four** substrate organization pools read live via `name_denylist.substrate_organization_pools()` (`INSURANCE_CARRIERS`, `DEFENSE_FIRMS`, `ALL_EMPLOYERS`, `MEDICAL_FACILITIES`) against manifests, document text and 200 generated casts; `zeroRealPii` computed from `CaseCast.provenance`, not asserted; seed-declared names kept but warned via `cast.seed_name_on_denylist`)
- [x] ISC-74: Anti: generation never writes outside `--out` (probe: sentinel `HOME`/`TMPDIR`/XDG tree snapshotted before and after a subprocess generate — created, modified and removed files all fail; **plus** the package `src/` and substrate source trees snapshotted for stray `__pycache__` — zero in the substrate, and only `__init__`'s own interpreter-written bytecode in the package, named explicitly rather than exempted by directory)

### Perspective (applicant vs defense files — added 2026-07-27 evening)
- [x] ISC-75: `CaseSeed.perspective` accepts `applicant | defense`, defaults to `applicant`; all pre-existing seeds load unchanged
- [x] ISC-76: Defense perspective makes the defense firm the file owner in the cast and `perspective` is recorded in the case manifest
- [x] ISC-77: Defense cases emit DEFENSE_TRIAL_BRIEF / DEFENSE_MSC_STATEMENT / DEFENSE_CASE_ANALYSIS as the privileged work product in place of applicant-side memos
- [x] ISC-78: Client correspondence targets the worker on applicant side and the employer/carrier contact on defense side (author/recipient roles in rendered docs + manifest)
- [x] ISC-79: Defense files emit CLAIMS_ADMINISTRATION and INVESTIGATION subtypes at materially higher frequency than mirrored applicant files (comparative probe)
- [x] ISC-80: Applicant-only material (client intake, advocacy letters to treaters, applicant client status letters) is absent from defense files by default, present only via explicit override
- [x] ISC-81: Demo caseload gains a defense-perspective case; all gates stay green (pytest, ruff, double-run + TZ determinism, validate)
- [x] ISC-82: Anti: flipping perspective never changes case facts — mirrored seeds (same rng_seed/injury/lifecycle) produce identical cast, ADJ number, and event dates (probe: manifest field diff)
- [x] ISC-83: README + user guide document perspective semantics and the defense-side docx letterhead limitation
- [x] ISC-84: Tests cover default, swap logic, applicant-only absence, and the mirrored-facts invariant

### Scenario seed v2, Phase 1 — CaseFacts ledger + coherence harness (AJC-37, iteration 3, added 2026-07-28)
- [x] ISC-85: `case_facts.py` defines a `CaseFacts` ledger (diagnostics performed/absent with modality+body part+date, surgery state + chosen CPT, provider list, treatment visit series, MMI date, WPI/PD figures) derived at plan time; double-derivation from the same seed is identical (probe: model dump compare)
- [x] ISC-86: `scenario.diagnostics {performed, absent}` accepted in `CaseSeed`; loader rejects performed/absent overlap and unknown modalities with actionable errors (probe: bad-seed pytest)
- [x] ISC-87: A seed with no `scenario` block auto-derives CaseFacts deterministically from the seed RNG (probe: double-run identity without scenario)
- [x] ISC-88: Per-case manifest carries a `caseFacts` block (diagnostics, surgery, providers) (probe: manifest pytest)
- [x] ISC-89: Renderer gains a fact-aware override registry (subtype → engine template subclass); subtypes not in the registry take the exact original code path (probe: dispatch assertion pytest)
- [x] ISC-90: `DIAGNOSTICS_IMAGING` rendered content names the ledger's modality for that document, never a private draw (probe: text extraction per performed diagnostic; refined 2026-07-28 — the substrate document prints all injury body parts joined and never attributes a study to one region, so per-study body-part attribution is unassertable against current output; deferred to Phase 2 with the diagnostic-report header work, see Decisions)
- [x] ISC-91: QME/AME report imaging citations name only ledger-performed diagnostics; a ledger-absent modality never appears (probe: extraction grep + positive control seeding `absent: [emg]`)
- [x] ISC-92: `scenario.surgery` drives `has_surgery` and every rendered characterization: `performed` → PR/QME text references the surgery and any emitted operative document names the ledger CPT; `none` → zero surgical language and `validate` rejects an operative document outright (probe: opposite-coin tests in both directions + validator reverse rule; refined 2026-07-28 — guaranteed operative-document *emission* is a substrate emission-rule property the ledger cannot honestly promise today, split to ISC-92.1)
- [x] ISC-92.1: [closed in Phase 2, commit 40d4024] A scenario-stated `surgery: performed` guarantees ≥1 operative document in the plan via a planner-level floor (the ISC-29 explicit-control philosophy applied to emission), closing the validator's forward direction

### Scenario seed v2, Phase 2 — treatment trajectory + diagnostics depth (AJC-37, added 2026-07-28)
- [x] ISC-101: `scenario.treatment.status` accepted: `ongoing | discharged | gap | never_treated`; loader cross-validates incompatibilities (never_treated rejects surgery ≠ none and medical/hospital/pharmacy lien claimants) with actionable errors (probe: bad-seed pytest)
- [x] ISC-102: `never_treated` → no treating reports, diagnostics, or billing beyond the first-report tier in the plan; a seeded QME notes the sparse record in rendered text (probe: plan assertion + extraction grep with positive control)
- [x] ISC-103: `gap` → the ledger's visit series contains the seeded gap and rendered PR/QME text references the treatment gap; visits before/after remain date-coherent (probe: ledger + extraction)
- [x] ISC-104: `discharged` → a discharge summary is emitted and no treating report post-dates the discharge date (probe: plan dates assertion)
- [x] ISC-105: Treating-report status phrases follow the seeded trajectory (improving / plateau / worsening) across the case's PRs instead of independent per-document draws (probe: multi-PR extraction, trajectory-coherence assertion + counterfactual)
- [x] ISC-106: `scenario.treatment.providers: N` sets the ledger roster size; manifest `caseFacts.providers` count matches and subpoena round-robin spans it (probe: manifest + extraction)
- [x] ISC-107: `surgery: recommended` → PR/QME text references the recommendation, zero operative documents, `validate` enforces their absence (probe: extraction + validator rule, positive control)
- [x] ISC-108: `surgery: denied_by_ur` → requires (or actionably rejects absent) a UR dispute; the RFA/UR-denial chain names the procedure; zero operative documents; `validate` rule (probe: chain extraction + validator)
- [x] ISC-109: ISC-92.1 closed — a scenario-stated `performed` guarantees ≥1 operative document via a planner floor; `validate` forward direction enforced (probe: plan floor test + validator biconditional)
- [x] ISC-110: `DIAGNOSTICS_IMAGING` attributes each study to its ledger body part in rendered text — the ISC-90 body-part clause rejoins (probe: per-study extraction)
- [x] ISC-111: The QME imaging-history section is governed — no invented modality anywhere in QME rendered text, including the history narrative (probe: extraction sweep with absent-modality control)
- [x] ISC-112: Every substrate content site that names a modality is enumerated in an audit table: each either governed by an override or documented-with-reason (probe: table-vs-grep test so a new site fails loudly)
- [x] ISC-113: Anti: seeds without `scenario.treatment`/new surgery values render byte-identical to v0.3.0 outside newly-governed subtypes; full byte accounting has 0 unexplained changes (probe: full-demo diff)
- [x] ISC-114: A scenario value overriding a substrate exclusion (death/psych surgery) emits a plan warning — keep-and-warn parity with denylist and doctrine-hook handling (probe: warning assertion; demo unaffected)
- [x] ISC-115: Version 0.4.0 with numbered compatibility notices; README + needed-seeds documentation cover the treatment block (probe: --version + doc grep)
- [x] ISC-116: Full suite + ruff green; substrate diff 0 files; demo determinism ×3; every new knob carries an opposite-draw counterfactual with a red-proven control (probe: suite + harness audit)

### Scenario seed v2, Phase 3 — personas + discovery (AJC-37, added 2026-07-29)
- [x] ISC-117: `scenario.adjuster.diligence` accepted: `attentive | ordinary | negligent`; unspecified derives deterministically on a facts-namespaced stream (probe: schema + double-derivation pytest)
- [x] ISC-118: Benefit-notice timeliness follows diligence — attentive inside statutory windows, negligent draws late against the same windows, and lateness is recorded as a ledger fact (probe: plan dates vs window table + ledger assertion)
- [x] ISC-119: A negligent-adjuster delayed claim emits a coherent delay chain (delay notice, follow-up correspondence density scaled by diligence) with fitted dates (probe: plan assertion per diligence level)
- [x] ISC-120: `PETITION_FOR_PENALTIES_LC_5814` is gated on ledger-recorded lateness, never a flat coin — a case with zero late benefit events cannot emit it, a seeded negligent case with recorded lateness can (probe: opposite-draw pytest both directions)
- [x] ISC-121: Adjuster-letter content type is coherent with the lifecycle — no UR-decision letter without a UR dispute, no settlement-discussion letter before the resolution window, at most one initial-acceptance letter per case (probe: rendered-letter sweep with positive controls)
- [x] ISC-122: `scenario.attorney.cadence` accepted: `every_30_days | event_driven | sporadic`; unspecified derives deterministically (probe: schema pytest)
- [x] ISC-123: `every_30_days` → client status letters on a ~30-day clock across the case span within tolerance; `sporadic` → irregular seeded spacing with ≥1 gap over 90 days (probe: plan-date cadence assertions)
- [x] ISC-124: `event_driven` → client report/analysis letters are dated relative to the events they discuss (report_date + n, resolution + n), never independent uniform draws (probe: per-letter anchor assertion)
- [x] ISC-125: Client-letter rendered content references its anchoring event when event-driven (probe: extraction grep with positive control)
- [x] ISC-126: `scenario.discovery.subpoena_sets` and `pages_per_set {min,max}` control subpoena packet count and declared page volume; the declared ToC page counts and rendered content page counts are drawn from the same seeded values, not independent dice (probe: manifest + extraction consistency)
- [x] ISC-127: Every new P3 validator checks the RESOLVED plan value, not the declared seed value, where the two can diverge — the declared-vs-resolved class is designed out, with a test per validator proving the adjacent path (probe: adjacent-path pytest per validator)
- [x] ISC-128: Modality audit pattern compiled case-insensitively; the ~6 newly-surfaced substrate lines reconciled into the table (each governed or documented-with-reason), including the diagnostic_report.py:69 "Radiographic examination" line in a governed template — with an explicit answer to whether it can assert a modality the ledger marks absent (probe: audit suite + the answer recorded in Decisions)
- [x] ISC-129: The seed-message half of the follows-the-message meta-guard is table-driven — every actionable message registered with its resolving edit, a registry-completeness check fails on an unregistered message (probe: registry pytest)
- [x] ISC-130: Adjuster/attorney persona facts published in the manifest caseFacts block only as governed facts documents actually honor (probe: governed-fields test extension)
- [x] ISC-131: Anti: seeds without scenario.adjuster/attorney/discovery render byte-identical to v0.4.0 outside newly-governed subtypes; full byte accounting 0 unexplained (probe: full-demo diff)
- [x] ISC-132: Demo determinism ×3 holds; version 0.5.0 with numbered compatibility notices; README + needed-seeds docs cover personas and discovery (probe: gates + doc grep)
- [x] ISC-133: Every new knob carries an opposite-draw counterfactual with a red-proven control; new hand-written lists carry liveness guards (probe: harness audit)
- [x] ISC-134: Full suite + ruff green; substrate diff 0 files (probe: suite + git diff)

### Phase 4 carry-ins from the PR #28 remediation sweep (added 2026-07-29 18:40 — measured defects, documented as limits in 0195f43, fixes deferred by design)
- [x] ISC-148: Discovery shaping respects document controls, loudly — an `exclude`/zero-override on a packet subtype is NOT refilled by `_shape_discovery` to the requested `subpoena_sets`; when a control suppresses packets the warning names the control, not the lifecycle stage (probe: the remediation's measured matrix — exclude → fewer packets + control-naming warning; reproduction in the 0195f43 README limit note) — **Resolved 2026-08-02, on the second attempt.** The refill defect turned out to need a *mixed* set to reproduce (one packet subtype surviving under an exact override, another suppressed); the emptied-file seed that found the warning defect never reaches it, because with nothing surviving there is no donor to clone. 23 tests in `TestDiscoveryShapingRespectsDocumentControls`, mutants `m12-1..m12-10`.

  **This criterion was ticked once while still false, and the tick is the finding.** A GPT-family review round proved by execution that the first fix reproduced the original defect through the *other* spelling of the same control: a seed writes `documents.overrides` as either `{subtype, count}` or `{type, max}`, `DocumentControls` splits them into two properties, and the fix read only `subtype_overrides`. So `{type: DISCOVERY, max: 0}` emptied the file and was blamed on the lifecycle stage — on a seed already at `discovery`. A second path did the same for `global_cap`, because the warning reached for the stage sentence whenever no per-subtype control matched, without consulting `stage_is_a_cause`. The trim side had never been touched at all and dropped an explicitly-counted subtype to zero in silence, and a redundant `exclude` prescribed removing whichever key sorted first. Then, taking the class rather than the instances, a *positive* `{type, max}` proved equally invisible to the refill — and, unlike an exact count, is a ceiling rather than a pin, so it must forbid cloning without forbidding a trim.

  What made the tick wrong was not the missed spelling; it was scoping the evidence to the seeds the fix was written against. The probes all used `{subtype, count}` because that is what the fix read, so the guard set and the criterion agreed with each other and neither touched the code. See the Changelog entry below.
- [ ] ISC-149: The three-readings guarantee holds across the full accepted `pages_per_set` range or the range is validated at the seed boundary — pagination floor discovered near 4 pages (requested 2, delivered 4, ledger unchanged); fix is a floor + post-measurement cover rewrite, not a warning; `packet_page_table_unstable` must be red-provable (it fired 0/27 in the sweep that found 6 ledger≠paper packets) (probe: budget sweep 1..120 re-run green + guard planted-control)
- [ ] ISC-150: The actionable-message detector handles displaced directive verbs ("You should set…", "Please set…", "We suggest you remove…") or the registry's scope statement enumerates the displacement hole exactly; `test_no_shipped_message_falls_into_the_hole` stays green as the interim guard (probe: detector pytest with the three reproduced evasions as fixtures)

### Money layer Wave 1 — the CaseFacts money spine (**AJC-43**, added 2026-07-30)
- [x] ISC-151: `scenario.wages` accepted and is **the gate** for the whole money layer — a history is either listed (`earnings[]`) or described (`pattern`/`base_weekly_wage`/`lookback_weeks`/`pay_frequency`/`overtime_share`), never both, and the loader rejects the mix with an actionable message (probe: schema pytest both ways + registry entry)
- [x] ISC-152: The wage block expresses every case that makes method selection a live legal issue — irregular earnings, concurrent employment, short employment history, overtime, in-kind wages — each reaching the derived ledger (probe: per-shape AWW recomputation, 7 histories)
- [x] ISC-153: `scenario.benefits` and `scenario.settlement` both require `scenario.wages`, through one gate, so "a seed with no wage block produces zero artifacts of this layer, and moves no output byte" is one checkable sentence rather than three (probe: bad-seed pytest per block)
- [x] ISC-154: AWW is **derivable, not asserted** — the published computation carries every operand (periods considered, weeks, gross, in-kind) and recomputing from them by the recorded method reproduces the published figure exactly (probe: recomputation sweep over 7 history shapes; explicit-earnings case pinned to a hand-computed 1000.00)
- [x] ISC-155: The AWW method is an explicit recorded field with five named values, a stated ordered selection rule, and the reason in words — never implied. `earning_capacity` is never derived (probe: per-method selection pytest with opposite draws; catch-all swept over 6 shape combinations)
- [x] ISC-156: The comp rate is reproducible from the wage facts by the named method under a **date-of-injury-keyed** basis honouring max/min, and *which bound bound* is recorded (`max`/`min`/`unbounded`) (probe: `compute_comp_rate` re-run over 4 wage levels; cap and floor each with an opposite draw; 4 injury dates yield 4 distinct basis+rate pairs)
- [x] ISC-157: No statutory number is presented as verified law — every fraction, cap, floor and date bracket lives in `UNCONFIRMED_RATE_TABLE`, carries `counsel_confirmed=False`, says `COUNSEL-UNCONFIRMED` in its own authority text, and publishes the flag in every manifest; a seed may supply a confirmed binding and the engine's table never can (probe: table sweep + seed-override pytest + manifest assertion)
- [x] ISC-158: `rate_basis_for(doi)` is the KB-167 seam — a plain function signature, no network, no configuration hook, no import of an authority package, so this ticket takes no dependency on the parallel repository (probe: the function is the only reader of the table; seam documented in module docstring + README)
- [x] ISC-159: The benefit ledger carries TD periods with dates and amounts and PD advances with dates paid; deliberate gaps and late payments are seed knobs (`td_gap_days`, `late_payments`, `max_days_late`) recorded as facts with their days (probe: gap/lateness pytest with opposite draws; amounts recomputed as rate × weeks)
- [x] ISC-160: Lateness and gaps default off `scenario.adjuster.diligence`, so the existing persona drives the money — `attentive` produces zero recorded lateness, `negligent` produces some — and the persona reaches the ledger through `build_case_plan`, not only through the deriver's argument (probe: opposite-draw pytest at both levels)
- [x] ISC-161: The settlement object is defined here (C&R vs Stips, gross, **approval date and funding date as separate fields**) so AJC-45 and AJC-46 can run in parallel; a non-settling resolution carries no settlement object (probe: per-resolution pytest incl. `findings_award`/`take_nothing` anti-draw; funding lag honoured at 0/30/180 days)
- [x] ISC-162: Determinism is absolute — every money value derives via `derive_seed` on a fresh `money:` namespace, never the global stream, never a `facts:` salt, never wall-clock; double-derivation is identical, varying a money knob moves no clinical fact, and the answer is independent of the ambient `decimal` context (probe: double-derivation, cross-knob clinical identity, precision sweep 2/3/5/6/9/28/40/50)
- [x] ISC-163: Money documents are a planner **floor**, not an emission — a subtype the walk already proposed is never doubled, the floor is measured to fire where the walk proposes nothing (`intake`), and `documents.exclude`/`include_only` still bind (probe: per-stage measurement + both control directions)
- [x] ISC-164: The documents carry the numbers — the wage statement prints the ledger's periods, AWW, named method, both rates and the counsel-unconfirmed basis; the payment record prints the gap and the days late; the C&R settles for the ledger's gross (probe: text extraction against the manifest's own figures, all three documents)
- [x] ISC-165: **Anti**: a seed with no wage block renders through the unmodified code path with zero artifacts of this layer, and moves no output byte — negative grep over a case forced to hold all four money-governed subtypes (an earned zero), no `money` key in manifest or `case_facts.yaml`, plus the code-path assertion that the subclass *is* dispatched and delegates; positive control proves the probe fires (probe: negative-grep pytest + dispatch assertion + full-demo byte diff 353/0)
- [x] ISC-166: `validate --out` refuses an uncheckable money claim in both directions — a published AWW with no wage statement in the folder, an ungoverned published field, an unknown method, a missing `counselConfirmed`, a funding date before its approval (probe: six planted violations, each proved to fire, manifest and artifact rewritten together so exactly one rule catches)
- [x] ISC-167: Version 0.8.0 with numbered compatibility notices; README + CHANGELOG cover the money block, the method table, the unverified-law warning and the seam; `examples/money-showcase.yaml` exercises all five methods, both bounds and four rate vintages (probe: `--version`, doc grep, showcase generate + validate + determinism ×3)
- [x] ISC-168: Every new knob carries an opposite-draw counterfactual, and the suite is **mutation-tested** — reverting the code under test turns the naming probe red (probe: 17-mutant run recorded in Verification)
- [x] ISC-169: **Anti**: the fabricated rating at `case_facts.py:915-916` is untouched and nothing in the rate derivation depends on `wpi`/`pd` — AJC-44's surface is left clean (probe: git diff on those lines; `money.py` contains no reference to either field)

### PR #29 cross-model review remediation (added 2026-07-30, from the Codex/GPT-5.6-Sol round-1 BLOCK)

Seven blocking findings were raised, each re-derived locally before it was acted on; two were refuted with evidence and are recorded under Decisions rather than fixed.

- [x] ISC-170: **Every exported callable is pinned against the ambient decimal context, and the sweep is driven off `__all__`** — `compute_aww` and `select_method` were exported, unwrapped and reached only through `derive_money_facts`, whose pin covered them from outside. Measured: `compute_aww` raised `InvalidOperation` at `prec` 2–5 and moved a published `gross_considered` from `58732.37` to `58732.30` at 6; a 26.2867-week history is labelled `actual_weekly_earnings` at 28 and `short_history_projection` at 2 (probe: `__all__`-driven precision sweep 2–50 asserting exported-callable identity, plus a threshold-adjacent label probe; both mutation-proven)
- [x] ISC-171: **Confirmation is a claim about numbers, so it takes the numbers** — `rate_basis: {counsel_confirmed: true}` published the entire engine-default table as counsel-confirmed under an authority string still reading `COUNSEL-UNCONFIRMED`. An override stating nothing now overrides nothing, and confirming requires all six figures plus the authority (probe: five-word laundering seed refused; an override short of exactly one figure also refused; empty override proven equal to the untouched table)
- [x] ISC-172: **A rate bound is stated with the bound it is bounded by, and the *merged* basis is validated** — a lone `td_min_weekly: 5000` merged under the $1,539.71 default ceiling and `compute_comp_rate` returned $5,000 recorded as `tdBound: min`, a rate above the maximum its own basis published. Closed at seed level by requiring the pair, and backstopped on `RateBasis` itself, which every construction path goes through — including the merge, rebuilt through the constructor because `model_copy(update=...)` skips validation (probe: unpaired and inverted seeds refused; the model probed directly so the seed rule cannot shadow it)
- [x] ISC-173: **All six wage shape knobs are mutually exclusive with a listed history, read from `model_fields_set`** — four carry defaults, so "is it None" could not tell an authored `pattern: irregular` from Pydantic's `pattern: regular`, and `pattern` is a *published ground-truth label*: a seed listing a steady history under `pattern: irregular` shipped a wrong label to the analyzer silently. A history of only concurrent periods is refused too — it divided a real $4,000 gross by zero primary weeks and published `averageWeeklyWage: 0.00` (probe: per-knob refusal including a knob restated at its own default; all-concurrent refusal with a mixed-history control)
- [x] ISC-174: **The benefit ledger publishes events, not only counts, and lateness is recomputable from the page** — `tdPeriods: 3` was a key named for a collection holding a cardinality, and changing it later would be a breaking change to an *extraction label*. Counts moved to `*Count`; the plural names now carry the records, every field of which is a column the payment record prints. `TdPeriod.date_due` is carried and published with `tdPaymentDueDays`, so `days_late` has its yardstick on the paper instead of in a module constant (probe: `daysLate` recomputed from published `dateDue`/`datePaid` on every record, totals recomputed from the arrays, opposite draw at `late_payments: 0`; `validate --out` cross-checks counts against arrays and re-derives every lateness)
- [x] ISC-175: **No payment record is dated before the payments it prints** — the money floor decided the date and then discarded it whenever the lifecycle walk had already proposed the subtype. Measured across 132 planned records on 30 seeds and 3 stages: **106 were dated before their own last payment**, one by 123 days. Already-proposed candidates are now re-dated forward — every candidate for the subtype, since the pool picks the earliest — and never backward (probe: the same 132-record sweep asserting `doc_date >= last payment`; a direct unit probe that a later candidate is left alone and an early one is moved)
- [x] ISC-176: **`earning_capacity` publishes the stated figure and nothing else** — it published the derived history's operands beside a number none of them produced (26 periods, 52 weeks, $51,830.08 "considered" for an AWW of 1500) and added in-kind wages on top, so the published figure was not even the one the author stated (probe: zeroed operands asserted, with a computing method on the same seed as the opposite draw proving the fields are not inert)
- [x] ISC-177: **Every governed field reaches the page, checked off the governance table rather than by hand** — `GOVERNED_MONEY_FIELDS` says a published fact is one a document renders; `pattern`, `methodSource`, `basisSource`, `basisAuthority` and both bound tokens were not. `unbounded` is the sharp one: it rendered as an empty parenthetical, so whether a rate was capped — the single most consequential fact about it — was recoverable from the page only when it happened to be capped. The table-driven probe found `methodSource` and the bound tokens after a manual pass had missed both (probe: sweep over `GOVERNED_MONEY_FIELDS['wage'|'rate']` asserting each published value appears in the extracted text)
- [x] ISC-178: **The settlement gross is whole dollars, once** — the substrate's release draws an integer and derives fee, costs, set-aside and net from it, so a ledger holding `88000.99` labelled a document reading `$88,000`. Refused at the seed rather than silently rounded, and quantized once after both derivation branches: a second copy on the stated branch was mutation-proven unreachable behind the validator, and a guard no probe can reach is an unverified claim that reads like one (probe: cents refused at the seed; 20 derived grosses asserted integral; both mutation-proven)
- [x] ISC-179: **A `funding_date` is stated with its `approval_date`** — a lone funding date was measured against an approval derived from the timeline, which the seed cannot see, so a 2024 funding date under a 2021 derived approval loaded cleanly and published a negative lag that only `validate --out` caught, one whole generation downstream (probe: refusal at parse time; the paired form still loads and records the interval)
- [x] ISC-180: **Every remediation above is mutation-proven** — 19 mutants, one per fix, each reverting the code to its pre-review state; **19/19 RED**. Three survived the first pass and all three were fixed rather than excused: a threshold-adjacent history replaced a comfortable one, the `RateBasis` backstop was probed directly instead of through the seed rule that shadowed it, and a redundant unreachable guard was collapsed into one provable call (probe: scripted mutate-run-restore over `money.py`, `seeds.py`, `planner.py`, `fact_templates.py`)
### PR #29 cross-model review, round 2 (added 2026-07-30, from the Codex/GPT-5.6-Sol round-2 BLOCK)

Round 2 audited the round-1 *remediation*, which is where the next defects were — including one the round-1 fix introduced. Seven findings, all seven re-derived locally; six accepted, one refuted.

- [x] ISC-182: **A listed history derives its own pattern, and `patternSource` records that it did** — forbidding the shape knobs beside explicit earnings (ISC-173) left `pattern` at its field default, so a history alternating $100 and $3,900 a fortnight published `pattern: regular` while the same facts selected `irregular_earnings_average`. One history, two contradictory published labels, and a **regression introduced by the previous round's fix**. Derived now from the same coefficient of variation `select_method` uses, so the two cannot disagree; `seasonal` is never derived, on the same argument that keeps `earning_capacity` out of the derived branch (probe: swinging history asserts `irregular`/`derived`, steady one asserts `regular`/`derived`, described one asserts `seasonal`/`seed`)
- [x] ISC-183: **`concurrent_aggregate` divides by the weeks both employers cover** — the denominator was the primary employment's own weeks for every method, right only when the histories share a calendar, which was documented and unchecked. A two-week primary period paying $2,000 beside a fifty-two-week concurrent history paying $52,000 published an average weekly wage of **$27,000**, capped to the statutory maximum and recorded as `tdBound: max`. Counting the union of covered days made the arithmetic reproducible where the two histories cover the same calendar, and **ISC-192 subsequently refused the unequal case outright** rather than publishing the union figure for it — $1,038.46 dilutes the primary across fifty weeks it did not exist. Read this criterion as the denominator fix for matched coverage only; ISC-192 owns the unequal case (probe: the pathological history asserts 52.0000 weeks and an unbounded rate; an aligned history asserts the primary span unchanged — every showcase figure held, which is the evidence the fix is a no-op where it should be)
- [x] ISC-184: **A stated benefit control is honoured or refused, never dropped in silence** — `{td_weeks: 0, pd_advances: 0, late_payments: 3, max_days_late: 62}` loaded and published `latePayments: 0`; `{td_weeks: 0, td_gap_days: 90}` published `gapDays: 0`. ISC-29's rule reaches the money knobs. What the seed alone cannot see — truncation against the case's own runway — is a **plan warning** instead, because a seed whose story outruns its calendar is not an impossible seed (probe: both shapes refused with registered messages; opposite draws on a run that holds them; `td_weeks: 520` warns and `td_weeks: 8` does not)
- [x] ISC-185: **No benefit event falls past the timeline horizon** — a loadable seed put a temporary-disability payment **588 days** beyond it, and `timeline.clamp` then dated the payment record before its own payment, because clamping cannot repair a future event. The rule the advances already followed now covers the periods, and covers advances by their *paid* date rather than only their schedule — a distinction that needed its own probe corner, since an advance's schedule is bounded by the benefit window and only a long delay carries it out (probe: sweep over 5 dates of injury × 3 stages/resolutions × 3 delays, asserting every TD and PD payment lands on or before the horizon, with a floor on how many advances the sweep must reach)
- [x] ISC-186: **A permanent-disability advance carries the schedule it slipped from** — the ISC-174 fix had been applied to the half a reviewer named rather than to the class, so four advances came back dated 08-30, 10-14, 11-28 and 11-11, three marked 62 days late, naming nothing to be late against. The apparent disorder is a real fact of a neglected file; it reads as one only once the schedule is on the page. `PdAdvance.date_due` is carried, rendered and published, and the ledger is ordered by it (probe: due dates ordered, `datePaid - dateDue == daysLate` on every advance, `dateDue` present in the published array)
- [x] ISC-187: **`validate --out` refuses an uncheckable ledger on both halves** — a case publishing four advances and no payment record passed clean; a TD event replaced by a bare string passed clean; PD lateness was never re-derived. The document rule now counts every event kind, non-mapping events are refused, unreadable dates are refused, and lateness is recomputed on both ledgers (probe: three planted violations, each proved to fire, each with a passing control)
- [x] ISC-188: **A settlement dated past the anchor is refused** — `approval_date: 2099-01-01` loaded and published a 2099 approval and funding beside documents dated 2026-01-01 or earlier: an event no paper in the folder could report (probe: refusal at parse time, registered message, followable resolution)
- [x] ISC-189: **The payment-due interval carries its own authority** — `TD_PAYMENT_DUE_DAYS` was the one number in the module with no authority beside it, and `rate.counselConfirmed` governs the rate basis rather than a benefits rule, so a consumer reading the interval under that flag was reading a caveat that did not cover it. `tdPaymentDueAuthority` and `tdPaymentDueConfirmed` are published and the authority prints on the payment record (probe: governed-field render sweep; manifest caveat assertions)
- [x] ISC-190: **Every round-2 fix is mutation-proven, and round 1 stays proven** — 13 new mutants, **13/13 RED**, plus a re-run of all 19 round-1 mutants, still **19/19 RED**: 32 of 32 across both rounds. One round-2 mutant survived first pass (the PD paid-date branch, unreachable from the sweep's own seeds) and the sweep was widened with a seed found by search rather than the finding excused (probe: two scripted mutate-run-restore campaigns)
### PR #29 cross-model review, round 3 (added 2026-07-30, from the Codex/GPT-5.6-Sol round-3 BLOCK)

Round 3 audited round 2. Six findings, all re-derived; the settlement tagged-union disagreement was conceded by the reviewer and is recorded under Decisions.

- [x] ISC-191: **`patternSource` names an author only when there was one** — `pattern` carries a default, so "the history was described" was being read as "the author stated a pattern", and every described history published `patternSource: seed` over a label Pydantic supplied. `model_fields_set` is the only thing that tells them apart (probe: unstated blocks assert `derived`; a pattern restated *at its own default* asserts `seed`)
- [x] ISC-192: **An unequal concurrent history is refused rather than averaged** — a two-week primary period at $2,000 beside a fifty-two-week concurrent history at $52,000 has no correct single denominator: 2 weeks gives $27,000, the union of covered days gives $1,038.46 (diluting the primary across fifty weeks it did not exist), and the defensible answer, $2,000, is the sum of two per-employment rates that a boolean `concurrent` cannot group. Refused, because publishing a number no arithmetic on the page reproduces would put the asserted figure this layer removes into the one method whose point is combining employments. Employer identity reopens the shape additively in Wave 2 (probe: unequal coverage refused; matched coverage recomputes from the printed table; every derived concurrent history unchanged, which is why no showcase figure moved)
- [x] ISC-193: **A figure no setting consumes is refused** — `earning_capacity_weekly: 7777` beside a described history published `996.73` under `actual_weekly_earnings`; `concurrent_weekly_wage: 8888` published no concurrent employment. Both fields document themselves as required by, and *only* by, something else, and both were accepted and discarded without it (probe: each refused; each honoured once its enabler is present)
- [x] ISC-194: **Lateness is honoured, refused, or reported — never dropped** — `max_days_late` alone took its count from the adjuster persona, so on an `attentive` file a stated 62-day delay published no lateness at all; the pair is now required. And eight weeks is two four-week blocks, so `late_payments: 3` published `latePayments: 2` in silence — a count the seed cannot check becomes a plan warning (probe: refusal for the unpaired knob; warning asserted with an opposite draw on a run long enough to carry it)
- [x] ISC-195: **A settlement the case never funded says so** — `approval_date: 2025-12-31` with `funding_days: 730` published a funding date of 2027-12-31 as fact, two years past the date this engine calls today, beside documents dated 2026-01-01 or earlier. Approved-and-not-yet-funded is a real state and `funding_date` is optional for it; the dropped lag is reported (probe: `funding_date`/`funding_lag_days` both `None`, warning on **both** return paths of `_money_control_warnings` — mutation found only one was probed)
- [x] ISC-196: **No gap outlives the run it interrupts** — the gap was banked when planned rather than when the period on its far side was emitted, so a run could end *on* a gap reaching past the horizon: `td_gap_days: 700` on a 2024-06-01 intake file banked one running to **2026-06-01**, five months out, printed on a payment record dated before most of it (probe: sweep over 4 dates × 4 gap lengths × 3 run lengths asserting every gap ends inside the horizon and has a period resuming after it, with a floor on how many gaps the sweep must produce)
- [x] ISC-197: **`validate --out` returns a verdict instead of crashing** — `tdPeriodCount: "one"` raised `TypeError` out of the event sum, and a crash is not a verdict; `gaps` was the one array nothing type-checked; an unpaid benefit could also record 62 days late, which is an effect with no second date behind it. Shape is checked before arithmetic now (probe: four planted violations, each with a passing control)
- [x] ISC-198: **The render-governance sweep asserts label *and* value** — found by review on my own probe: `methodSource` and `patternSource` are both usually `derived`, so deleting the pattern-source row stayed green on the method-source row's text. A sweep a duplicate value can satisfy is not checking the field it names. The sweep also forced the in-kind row unconditional, since `inKindWeekly` is published unconditionally and a row that appears only when the value is interesting is a label an analyzer cannot recover at zero (probe: the sweep itself, mutation-proven against three deleted rows)
- [x] ISC-199: **Three mutation campaigns, all green** — 13 new mutants **13/13 RED**, plus re-runs of round 1 (**19/19**) and round 2 (**13/13**): **45 of 45**. Two round-3 mutants survived first pass, both because the sweep could not reach the branch rather than because the guard was wrong, and both sweeps were widened with cases found by search (probe: three scripted mutate-run-restore campaigns, all re-run on the final tree)

### PR #29 cross-model review, round 4 (added 2026-07-30, from the Codex/GPT-5.6-Sol round-4 BLOCK)

Round 4 audited round 3, and found that the settlement — the group round 3 had spent most of its findings on — was the one group the manifest validator skipped entirely. Five findings, all five re-derived locally and accepted; the tagged-union disagreement carried from round 3 was withdrawn by the reviewer.

- [x] ISC-200: **A settlement is governed like every other group** — `_validate_money` skipped the whole `settlement` key rather than the *absent* settlement it meant to skip, so the one group whose fields round 3 had just added was the one group nothing shape-checked or governance-checked. `GOVERNED_MONEY_FIELDS` says a published fact is a promise the documents keep; for a settlement the promise was never read (probe: a settlement missing a governed field refused, with a passing control; the absent-settlement path still skipped)
- [x] ISC-201: **The release prints the dates the manifest publishes** — forcing the gross onto the page was not enough. `approvalDate`, `fundingDate` and `fundingLagDays` were published as ground truth and appeared on no paper in the folder: the substrate's release leaves its approval line blank and carries no funding date, and the payment record that could print them exists only when the case paid benefits. Measured — a settled case with `td_weeks: 0, pd_advances: 0` published all three and `validate --out` passed it. The interval between approval and funding is the entire substance of a late-funding argument, which is the stated reason these are two fields and not one, so a corpus publishing the interval and printing it nowhere holds the label without the evidence. The C&R family now appends its own terms table, and the validator refuses the dates when neither a settlement document nor a payment record is in the folder to read them from (probe: the released case's extracted text asserted to carry both dates; the benefit-free settled case refused, with a control that holds a release)
- [x] ISC-202: **`concurrent_aggregate` names an aggregate that happened** — a seed may name any method, because the author's argument beating the engine's rule is why `method` is authored at all. This one differs in kind: the other four label an *argument* about how to average one history and could be argued over any history, while `concurrent_aggregate` labels a *fact* — that earnings from more than one employer were combined. Over a single employment there was nothing to combine, and the seed loaded and published `method: concurrent_aggregate` beside `concurrentEmployment: false`, a ground-truth label the manifest contradicts one field later (probe: refused without concurrent employment, honoured with it, and honoured when a listed history marks the second employer's periods instead)
- [x] ISC-203: **A count that is a count, and a gap that runs forwards** — `isinstance(value, int)` admits `bool`, so `tdPeriodCount: True` passed the type check and then compared equal to a length of 1; a negative count passed everything. And `spanned == days` agreed with a reversed gap carrying a negative count: 2025-01-10 to 2025-01-01 spans -8 days and recorded -8, two wrongs cancelling into a clean bill of health (probe: bool, negative and reversed planted separately, each with a passing control)
- [x] ISC-204: **The funding lag is re-derived, and a non-date is refused rather than raised** — `fundingLagDays` was published, never checked, and trivially disprovable from the two dates beside it; and a settlement date that was not a string crashed the validator out of the `<` comparison, which is the same defect ISC-197 fixed for benefits, in the group ISC-200 had been skipping (probe: a wrong lag refused, a non-date refused as a verdict rather than a traceback, both with passing controls)
- [x] ISC-205: **Four mutation campaigns, all green** — 8 new mutants **8/8 RED**, plus re-runs of rounds 1 (**19/19**), 2 (**13/13**) and 3 (**13/13**): **53 of 53** on the final tree. The re-run earned its keep twice: round 3's count-type mutant reported PATCH-MISS because ISC-203 had rewritten the very line it patched, and round 2's missing-payment-record mutant survived because ISC-201's new rule *also* says "payment record", so the probe was passing on a message a different rule raised. The mutant was refreshed and the probe re-pointed at the message it names — a probe another rule can satisfy is not checking the rule it names (probe: four scripted mutate-run-restore campaigns, all re-run on the final tree)

### PR #29 cross-model review, round 5 (added 2026-07-31, from the Codex/GPT-5.6-Sol round-5 BLOCK)

Round 5 audited round 4 and found that round 4's own headline fix was wrong in the way its predecessor was right: it put three published dates on a document that could not know them. Four findings, all four reproduced locally and accepted.

- [x] ISC-206: **A published date is printed by a document dated on or after the event** — round 4 put the approval and funding dates on the compromise and release, and a C&R is signed and filed *before* the Board approves it. Measured: a release dated 2023-11-13 printing an approval of 2023-12-27 and a funding of 2024-01-10 — 44 and 58 days in its own future — and `validate --out` passed it. The alternative round 4 also admitted was worse: a temporary-disability payment record dated 2021-09-01 printing the same 2023 approval. An anachronism is not weaker evidence than a missing line, it is worse, because it reads as evidence. Each date now belongs to the document that effects it — the release carries type and gross, `ORDER_APPROVING_SETTLEMENT` carries the approval it issues on, `BENEFIT_PAYMENT_LEDGER` carries the funding and the interval — and the money floor guarantees both carriers rather than hoping the walk supplied them (probe: every governed settlement field asserted on its own carrier's page with the carrier's date asserted at or after the event; the release asserted *not* to name either date; the right subtypes dated one day early still refused)
- [x] ISC-207: **The governance table binds in both directions** — the loop refused ungoverned extras and never required the governed fields, so deleting `settlement.grossAmount`, `wage.averageWeeklyWage` or `benefits.gaps` from a valid manifest was certified clean. Every field in the table is emitted unconditionally, nullable ones as `None` and never absent, so a missing key is a lost extraction label rather than an optional one — and ISC-200's recorded probe claim that a settlement missing a governed field was refused was simply **not true**, which is why it is restated here rather than quietly corrected. The lag is covered too: with both dates in hand the interval is derivable, so a `null` beside them is a dropped fact (probe: table-driven, every field of every group deleted in turn — 39 deletions, each refused by name, on a control that passes)
- [x] ISC-208: **`basisSource` says how much of the binding was authored** — a partial override published `source: seed` for the whole basis beside a `basisLabel` still naming the engine vintage the other five figures came from. `rate_basis: {td_fraction: 0.5}` authored one number of six and claimed all of them. As an extraction label that is meaningless: `seed` would have to mean "between one and six of these were authored". Three values now, and `authority` — prose *about* the numbers rather than one of them — cannot promote a partial binding (probe: six override shapes asserted against expected provenance; the merged basis asserted to keep the five figures it did not state)
- [x] ISC-209: **`latePaymentCount` finishes the rename ISC-174 started** — `tdPeriods: 3` became `tdPeriodCount` because a plural name holding a cardinality is an extraction label that has to break later; `latePayments` was left behind in that rename and is an integer under a plural name. Renamed now, while the vocabulary is still unfrozen and the four sibling tickets have not attached to it (probe: the governance sweep, which enumerates the table)
- [x] ISC-210: **Every round-5 fix is mutation-proven, and rounds 1–4 stay proven** — 10 new mutants, **10/10 RED**, plus re-runs of rounds 1 (19/19), 2 (13/13), 3 (13/13) and 4 (8/8): **63 of 63** on the final tree. One round-5 mutant survived first pass — disabling the floor's approval carrier changed nothing, because for a *derived* settlement the walk happens to date its own order on the approval day. The guard was not redundant; the sweep was blind. An **authored** approval date is where they diverge, and search found the walk dating an order 2021-10-09 against an authored approval of 2024-06-03 — nearly three years early — on 80 seeds. The sweep was widened rather than the survivor excused (probe: five scripted mutate-run-restore campaigns, all re-run on the final tree)

### PR #29 cross-model review, round 6 (added 2026-07-31, from the Codex/GPT-5.6-Sol round-6 BLOCK)

Round 6 audited round 5 and found round 5's own carriers were the wrong documents — the fifth consecutive round to find a defect inside its predecessor's fix. Eight findings, all eight reproduced locally and accepted.

- [x] ISC-211: **The stipulated award states the ledger's money, not its own** — `FactAwareCompromiseAndRelease` had no counterpart for the other resolution type, so every `stipulations` case shipped its *primary settlement document* computing money from scratch: an average weekly wage from `hourly_rate * weekly_hours`, a temporary-disability run from `random.randint(4, 52)`, a rate from a hardcoded 0.67 and an award from `random.randint(5000, 75000)`. Measured on the shipped `steady-earner`: `caseFacts` published 1151.42 / 767.65 / 26867.75 and the document beside it read 1331.20 / 891.90 / 40135.68. Two money ontologies in one case, on the page an analyzer is trained against — this layer's founding defect, sitting on the document that matters most to that branch (probe: the published AWW, TD rate, TD total and gross all asserted on the stipulated award's own page)
- [x] ISC-212: **The order approving the settlement says a settlement was approved** — `MinutesOrders` ignores its `approving_settlement` variant and draws proceedings from a pool about ongoing litigation, so round 5's carrier printed *"The parties have been unable to reach a settlement agreement at this time"* directly above a `Date Approved` row. Counter-evidence is worse than absent evidence: a missing line is a gap, a contradicting line is a fact the corpus asserts against itself. The substrate's WCAB caption is right and is kept; everything from its title down is replaced with the order this subtype names (probe: approval language asserted, litigation body asserted absent, the document's own date asserted equal to the approval)
- [x] ISC-213: **The benefit ledger is a benefit ledger** — round 5's funding carrier subclassed `BillingRecords`, a *provider* statement of charges with a doctor's letterhead, random CPT codes and a balance due, so a benefit-free settled case evidenced its settlement funding with a $16,724.23 medical bill. The date was right and the document was about somebody else's money. Rebuilt as the claims administrator's own ledger from the substrate's letterhead and claim-reference helpers: every TD period, every PD advance, the settlement disbursement and the totals the manifest publishes (probe: ledger title asserted, `STATEMENT OF CHARGES` and `BALANCE DUE` asserted absent, published totals and funding date asserted on the page)
- [x] ISC-214: **No payment record reports the settlement's future** — `_rewrite_benefit_record` appended both settlement dates to *every* payment record, and a payment record is dated when the benefits it lists were paid: one dated 2021-09-29 carried an approval of 2023-12-27, **819 days** in its own future. Round 5 established the rule and repaired only the release; this older path was untouched, which is why the rule is now stated once and the dates live only on the two carriers (probe: every payment record asserted free of settlement rows, with a floor on how many predate the approval so the probe cannot pass vacuously)
- [x] ISC-215: **The approving order is dated *on* the approval, not merely on or after it** — the floor re-dated forward only, which is right for a document that *reports* events and wrong for one that *constitutes* an event. An authored approval of 2022-01-05 was evidenced by an order dated 2023-12-27, 721 days later, and the on-or-after rule certified it. Pinned in both directions and checked for equality (probe: authored approvals on each side of the walk's own order date, so the guard cannot pass by only ever moving one way; the validator refused a day early and a day late, and `>=` would have passed the latter)
- [x] ISC-216: **An authority-only override authors no figure** — `rate_basis: {authority: "..."}` published `basisSource: mixed` having stated none of the six numbers, because the empty-block early return is keyed on *any* change and prose is a change. The prose is still adopted — it is the citation for these numbers — but the provenance of the numbers is unchanged, which is the whole content of the field (probe: the provenance table gained the authority-only row and now covers all three values)
- [x] ISC-217: **`basisSource` is checked against its own vocabulary** — `method` and both bound tokens were validated on output and `basisSource` was not, so `"seed-ish"` was certified as long as the manifest and the artifact agreed with each other. Two artifacts agreeing on a value outside the vocabulary is a copy, not a check (probe: all three labels planted with out-of-vocabulary values, each refused, on a control that passes)
- [x] ISC-218: **A fatal claim refuses the money blocks** — `scenario.wages` on a death loaded cleanly and derived ordinary disability from it: a death on 2023-01-19 produced a first temporary-disability period running 2023-01-22 to 2023-02-18, begun three days after the worker died, thirteen periods totalling $39,133.85 and two permanent-disability advances. Temporary disability replaces wages the worker would have earned and permanent disability rates a living worker's residual capacity; neither survives the worker, and what a fatal claim actually pays — dependency benefits and burial expenses — is a different computation with no model here. Refused rather than approximated, on the rule that governs the rest of this schema: an impossible seed is rejected, not absorbed (probe: three money shapes refused with a registered followable message; death without money still loads and derives no money facts)
- [x] ISC-219: **Six mutation campaigns, all green** — 10 new mutants **10/10 RED**, plus re-runs of rounds 1 (18/18), 2 (13/13), 3 (13/13), 4 (8/8) and 5 (10/10): **72 of 72** on the final tree. The re-run earned its keep a third time: round 1's `if not changes: return basis` short-circuit stopped going red, because ISC-216's three-valued provenance computes the same answer on the same inputs and removing the short-circuit became undetectable. Collapsed into the one provable path — the same call round 1 made on the redundant `_whole_dollars` quantization, on the same argument (probe: six scripted mutate-run-restore campaigns, all re-run on the final tree)

### PR #29 cross-model review, round 7 (added 2026-07-31, from the Codex/GPT-5.6-Sol round-7 BLOCK)

Round 7 audited round 6. Three findings, all three reproduced locally and accepted. Two were defects round 6 introduced — the sixth consecutive round — and one of them was invisible to round 6's own probe for the reason the reviewer named as a fourth failure class.

- [x] ISC-220: **The stipulated award's average weekly wage is substituted as a whole money token** — the rewrite partitioned the sentence on the next `"."`, which is the **decimal point of the substrate's own figure**, so `$1331.20.` became `$1,001.23.20.`: a mangled number published as ground truth. The probe stayed green because it asserted `aww in page`, and `"1001.23"` is a substring of `"1001.23.20"`. Substituted by pattern now, with a logged miss if the match count is not exactly one — and the probe **parses** the amount and compares it numerically, with a lookahead that rejects a digit or a point-then-digit after the cents (probe: every money figure on that page parsed and compared as a `Decimal`, not searched for)
- [x] ISC-221: **`settlement.grossAmount` means the total resolution value on both branches** — round 6 forced the gross into the substrate's `base_pd_award`, where the page labels it *"Permanent Disability (Gross)"* and then adds a self-procured medical reimbursement on top. The same number was therefore the whole settlement in the manifest and one component of the award on the page, and the stated cash obligations summed past the published total. A shared extraction key needs one stable meaning, and the meaning it has on the release is the total. The two cash components are reconciled to it — the award is the gross less the reimbursement, both pinned — and the total prints under its own label so the sum is checkable rather than believed. The SJDB voucher is a training benefit rather than money out of the award and is left alone; disbursement past that line stays AJC-46's (probe: the printed award plus the printed reimbursement asserted **equal** to the published gross, all three parsed)
- [x] ISC-222: **The settlement chain is ordered, not merely each link** — pinning the order to an authored approval (ISC-215) satisfied its own relation and broke the one before it: an approval of 2022-01-05 put the order **677 days** before the stipulations it recites as "filed herein", and the funding ledger before the agreement it funded. Every local check passed and the sequence was impossible. The chain `instrument ≤ approval == order ≤ funding ≤ ledger` is now planned as a chain — the instrument is pulled back only when it would otherwise post-date its own order, and never before the claim it settles — and `validate --out` refuses the inversion whatever produced it (probe: three authored approvals × two resolutions asserting the whole chain, plus a planted instrument one day after its approval refused and two controls)
- [x] ISC-223: **Seven mutation campaigns, all green** — 6 new mutants **6/6 RED**, plus re-runs of rounds 1 (18/18), 2 (13/13), 3 (13/13), 4 (8/8), 5 (10/10) and 6 (10/10): **78 of 78** on the final tree. Three round-5 and round-6 mutants reported PATCH-MISS because round 7 rewrote the exact lines they patched; each was re-pointed at the surviving logic rather than dropped (probe: seven scripted mutate-run-restore campaigns, all re-run on the final tree)

### PR #29 cross-model review, round 8 (added 2026-07-31, from the Codex/GPT-5.6-Sol round-8 BLOCK)

Round 8 audited round 7. Two findings, both defects round 7 introduced, and both are the same mistake in different clothes: **a bound applied to the wrong term**.

- [x] ISC-224: **The chain's floor belongs on the approval, not on the instrument** — ISC-222 pulled the instrument back to meet an authored approval and floored it at the claim, and then `min(filed, approval)` defeated that floor whenever the approval was earlier still. Measured: `approval_date: 2021-06-15` on a case whose claim was filed 2021-06-27 and whose Application was filed 2021-10-05 produced a compromise and release dated 2021-06-15 — reciting an Application that would not exist for eleven weeks, and predating the claim it settles. The floor now sits on the *approval*, where the chain actually starts, anchored to the Application the instrument recites plus the lead the instrument needs; and because an authored control was moved, it is **reported as a plan warning** rather than adjusted in silence, on ISC-184's rule (probe: both resolution types asserted moved past claim and Application, the warning asserted by name and date, the whole chain re-asserted, and a feasible authored approval asserted untouched with no warning)
- [x] ISC-225: **The award reconciles at every gross the schema accepts** — ISC-221 clamped the reimbursement into the substrate's own `randint` bounds and abandoned the reconciliation when the award fell below 5000, leaving both draws random. A gross of `5499` printed components totalling **$32,696** beside a published $5,499, with no warning and a clean `validate --out`; the derived branch reached it too. Those bounds select *which draw to answer* and never constrained the answer — treating them as a domain was the error. A five percent reimbursement, floored at a dollar and capped so the award keeps one, reconciles down to a two-dollar settlement (probe: five grosses from $2 to $88,000 including both sides of the old boundary, each asserting the printed components sum **exactly** to the published gross)
- [x] ISC-226: **Eight mutation campaigns, all green** — 5 new mutants **5/5 RED**, plus re-runs of rounds 1 (18/18), 2 (13/13), 3 (13/13), 4 (8/8), 5 (10/10), 6 (10/10) and 7 (6/6): **83 of 83** on the final tree, with no PATCH-MISS in any campaign (probe: eight scripted mutate-run-restore campaigns, all re-run on the final tree)

### PR #29 cross-model review, round 9 (added 2026-07-31, from the Codex/GPT-5.6-Sol round-9 BLOCK)

Round 9 audited round 8. Three findings, all three consequences of round 8's own fixes, and all three the same shape: **a change that was right about its own term and silent about what depended on it**.

- [x] ISC-227: **A stated funding date travels with the approval it hangs off** — ISC-224 moved an infeasible authored approval forward and left an authored `funding_date` where it was, publishing `fundingLagDays: -117`: money moving four months before the Board approved it, which is the exact impossibility ISC-179 paired these two fields to prevent. The pair states an *interval*, so moving one end silently rewrites the interval into something the author never asked for; both ends now move by the same shift and the interval survives (probe: the authored 16-day interval asserted intact after a 133-day forward move, on both resolution types)
- [x] ISC-228: **A settlement is large enough for its own document** — `gross_amount: 0` and `1` were accepted while the stipulated award silently skipped its reconciliation for them and printed a **$27,581** award beside a published $0.00. ISC-225's "unreachable from a sane seed" was an unverified claim of exactly the kind this review keeps punishing: it is not a boundary when `parse_case_seed` accepts the value. The floor is derived rather than invented — two whole-dollar cash components summing to the gross, over an award divisible by twenty — and a *stated* gross below it is refused with a registered message while a *derived* one is raised, because a derivation is not an author's instruction. The derived path is reachable and was found by search: `pd_min_weekly: 0` with `pd_max_weekly: 1` gives a rate of $0.67 and a raw gross of $13 (probe: 0, 1 and 20 refused with a control at 21; the authored-basis seed asserted raised to the floor with an ordinary case as the opposite draw)
- [x] ISC-229: **The fifteen percent fee is fifteen percent** — the substrate truncates `award * 0.15` to an integer, so an award of $5,225 printed *"a reasonable attorney fee of 15% … which equals $783"* for a true $783.75: a sentence the page contradicts. Pre-existing in the substrate and made reachable by ISC-221's split, which is why it is fixed here rather than deferred — the award is engine-owned now, so its consequences are too. The reimbursement is chosen congruent to the total modulo twenty, leaving an award divisible by twenty whose fifteen percent is exact, and still nearest five percent of the total so the document reads like a real file (probe: five grosses asserting the printed fee **equals** fifteen percent of the printed award, alongside the reconciliation)
- [x] ISC-230: **Nine mutation campaigns, all green** — 5 new mutants **5/5 RED**, plus re-runs of rounds 1 (18/18), 2 (13/13), 3 (13/13), 4 (8/8), 5 (10/10), 6 (10/10), 7 (6/6) and 8 (5/5): **88 of 88** on the final tree. Three of the five round-9 mutants **survived first pass**, and all three were the same failure — I had pointed them at tests that did not cover the fix. Real guards were written for each rather than the mutants retargeted at something convenient, and three superseded round-6/7/8 mutants were re-pointed at the surviving logic (probe: nine scripted mutate-run-restore campaigns, all re-run on the final tree)

### PR #29 cross-model review, round 10 (added 2026-07-31, from the Codex/GPT-5.6-Sol round-10 BLOCK)

Round 10 audited round 9. Three findings, plus a fourth found while reproducing the first — and that fourth is the one that mattered most, because it was live on the shipped showcase.

- [x] ISC-231: **The release's deductions come out of the settlement they are deducted from** — the substrate draws costs from $500 to $3,000 and a Medicare set-aside from $5,000 to $25,000 with no reference to the gross, so a $21 settlement printed **Net to Applicant $-17,608**: a release stating that the applicant owes money for settling. Both are pinned as fractions of the gross, which keeps the net positive by construction and keeps the release's own arithmetic reproducible from its page (probe: four grosses asserting the printed gross, fee, costs and set-aside reconcile to the printed net, and that the net is positive)
- [x] ISC-232: **A fifteen percent fee is fifteen percent on *both* documents** — ISC-229 fixed this sentence on the stipulated award and left it false on the release, which is the more important document, and **it was false on shipped cases**: a gross of $32,668 printed a fee of $4,900 against a true $4,900.20; $13,810 printed $2,071 against $2,071.50. Found while reproducing ISC-231 rather than reported, which is the argument for reproducing a finding instead of accepting it. Both documents truncate `gross * 0.15`, so settlement grosses are now a whole multiple of twenty — the coarsest step for which fifteen percent is always whole — refused at the seed with a registered message and rounded in the derivation (probe: both document families, four grosses each, asserting the printed fee **equals** fifteen percent of the printed base)
- [x] ISC-233: **A dropped-funding warning names the control that dropped it** — the warning knew only about `funding_days`, so a stated `funding_date` carried past the horizon by a forced approval was reported as "the funding lag for this adjuster", a control the seed never mentioned and the author cannot act on. A warning naming the wrong knob is not followable, which is the entire purpose of one (probe: the stated-date case asserted to name the date and *not* the adjuster, with the stated-lag case as the opposite draw)
- [x] ISC-234: **"Nearest five percent" is asserted, not described** — mutation found the claim unguarded: shifting the reimbursement one step keeps both the sum and the exact fee, so nothing was checking the property the docstring names. The selection also had a real defect behind it — the target was floored before distances were compared, so a total of $221 chose $1 when $21 was nearer to $11.05 (probe: the chosen reimbursement asserted equal to the nearest valid candidate, computed independently in the test)
- [x] ISC-235: **Ten mutation campaigns, all green** — 5 new mutants **5/5 RED**, plus re-runs of rounds 1–9: **93 of 93** on the final tree. Two of the five round-10 mutants survived first pass and one more did after the fix, all three because the mutant named a test that did not cover the fix — the same failure round 9 recorded, repeated, which is why the campaign is re-run rather than trusted. Round 1's cents mutant also stopped going red: the twenty-dollar step drops the cents on its way through `int()`, so the `_whole_dollars` call above it became undetectable and was collapsed (probe: ten scripted mutate-run-restore campaigns, all re-run on the final tree)

- [x] ISC-236: **The twenty-dollar lattice on `gross_amount` is withdrawn, and the fee prints to cents** — review was right that narrowing a *published* field to work around a renderer's rounding makes the corpus systematically unrealistic in a figure the analyzer is scored on. Real settlements are not multiples of twenty. Withdrawing it exposed what it had been hiding: the release's fee *sentence* had never been corrected at all, because round 9's substitution matched on the substrate's bold wrapper and only the award bolds its figure. A whole-dollar fee made the miss invisible, since a truncated fee and an exact one differ only in the cents the lattice removed (probe: both documents, four grosses each, asserting the table fee, the prose fee and the prose costs agree with each other and with fifteen percent of the printed base)
- [x] ISC-237: **The settlement floor is searched from the deduction rule, not chosen beside it** — $2 satisfied the award's two-line split and left the release printing **Net to Applicant $-0.30**: the floor was answering one document about a constraint the other one binds. `settlement_deductions` is now the single definition of the fee, the costs and the set-aside, imported by the renderer that prints them and by the search that derives the floor from them, so a divisor moving moves the floor with it (probe: the floor asserted to be the smallest gross whose deductions leave a positive net, with the dollar below it asserted not to, and the raised message asserted to name the derived number)

- [x] ISC-238: **The mutation gate is mechanized and runs on every push** — ten rounds ran the campaigns by hand from throwaway scripts at the end of each round, and three separate rounds discovered a guard that had never covered its own fix (round 9 diagnosed it; round 10 reproduced it identically). Ninety-three mutants are now a checked-in registry, one per shipped fix, each naming the edit that reintroduces the defect and the test that must redden. A mutant that no longer applies is a hard failure, because a PATCH-MISS means the code under a guard moved and nobody re-checked that the guard still reaches it first run **93 of 103**: nine PATCH-MISSes — guards that had quietly stopped reaching their code when round 11 withdrew the lattice — and one survivor, which was a sweep's own guard failing to cover its own fix. Six re-pointed, three retired naming their successors, and four real guards written for the survivors rather than the mutants retargeted. Final: **100 of 100 red** (probe: `tools/mutation_gate.py` over `tests/mutants.toml`, sharded ten ways in CI)
- [x] ISC-239: **The document/money matrix is derived from the registry, not from the overrides** — a matrix built from the fact-aware list can only confirm what is already governed, which is how a twenty-seven-subtype template family making nine money draws went eleven rounds without anyone looking at it. Every money-drawing substrate module is now either governed for every subtype routed to it or listed with a reason, and the ledger fails in both directions (probe: `TestEveryMoneyDrawingTemplateIsAccountedFor`, plus a staleness check that fails when an entry stops being true)
- [x] ISC-240: **A governed money fact is the same figure on every surface that prints it** — the sweep renders one settled case and walks every document in it rather than a named few. It found five defects on its first run: the settlement memo family's parallel money ontology (a temporary-disability rate of $1,141.07 against a published $767.59, a recommended target of $387,466 against an executed $32,668, a hardcoded PD rate of 290), a range-table row whose columns summed to $432,073 beside its own printed total of $427,428, the wage family governed on eight emittable subtypes of ten, with `PRIOR_CLAIMS_EDD_SDI_INFO` printing a temporary-disability rate of $848.53 against a published $767.59, a free `random.uniform(18, 55)` behind `employer.hourly_rate` feeding three document families, and a personnel-file promotion row raising pay above the rate the file says the applicant earns (probe: `TestAGovernedFigureIsTheSameOnEverySurface`, which also fails when a rule matches nothing). The registry binding is over the *canonical* subtypes: the first run mapped five registry-only keys the engine can never emit, and `test_scenario_p2` refused them — a hand-written list would have been wrong in the other direction too

### AJC-62 final mutation-gate remediation (added 2026-08-14)

- [x] ISC-241: Mutant `m20-3` fails its named guard on an assertion (probe: isolated mutation gate).
- [x] ISC-242: Mutant `m20-25` fails its named guard on an assertion (probe: isolated mutation gate).
- [x] ISC-243: Mutant `m20-26` exposes individual clamping through its named assertion (probe: isolated mutation gate).
- [x] ISC-244: Mutant `m20-28` exposes cross-family variant activation through its named assertion (probe: isolated mutation gate).
- [x] ISC-245: Mutant `m21-8` exposes sparse IMR commentary through its named assertion (probe: isolated mutation gate).
- [x] ISC-246: Mutation shard 3 reports every registered mutant killed by assertion (probe: shard gate).
- [x] ISC-247: Mutation shard 5 reports every registered mutant killed by assertion (probe: shard gate).
- [x] ISC-248: Mutation shard 6 reports every registered mutant killed by assertion (probe: shard gate).
- [x] ISC-249: Mutation shard 8 reports every registered mutant killed by assertion (probe: shard gate).
- [ ] ISC-250: Full pytest suite exits zero (probe: requested pytest command).
- [ ] ISC-251: All five golden corpora report OK (probe: requested golden command).
- [ ] ISC-252: Ruff reports `All checks passed!` (probe: requested Ruff command).
- [ ] ISC-253: Anti: No mutant is weakened to make its guard easier to kill (probe: registry read-back).

- [x] ISC-181: **Anti**: the whole-corpus byte accounting survives the remediation unchanged — **353 files, 345 identical, 8 changed, 0 unexplained**, all 331 rendered documents byte-identical, no `money` key anywhere in the demo output, substrate tracked diff 0 (probe: `git archive origin/main` baseline regenerated and diffed against HEAD after every fix above; determinism ×3 including `TZ=Australia/Sydney`)

### Phase 3a confirmation residuals + prose-truth guard (added 2026-07-29, from the PR #25 confirmation review)
- [x] ISC-135: The control-suppression warning is keyed off "ledger-earned but absent from the final plan", not off which control name matched — a ledger-earned petition suppressed by a zero-count override, `global_cap`, a parent-type exclude, or include_only-plus-perspective warns exactly as an explicit exclude does; each adjacent path red-proven (probe: per-path pytest; reviewer proved zero-count override and global_cap suppress silently today)
- [x] ISC-136: The petition-date bounds conflict is decided explicitly — `max(horizon, latest_late_event + 1d)` is not a clamp when the late event sits at/after the horizon; either the winning invariant is asserted with a constructed boundary seed proving the losing bound yields, or unreachability is proven and recorded (reviewer: 0/200 negligent seeds reproduce; unreproduced ≠ disproved) (probe: boundary-seed pytest or recorded unreachability proof)
- [x] ISC-137: Docstring meta-guard closes the code-vs-prose class — every schema field whose docstring carries the "not yet honoured" marker is asserted byte-inert (varying it changes no output byte), honoured fields must not carry the marker, and the guard fails red when a marker is planted on an honoured field (probe: marker-sweep pytest with positive control; would have caught the original M2 defect automatically)
- [x] ISC-93: The chosen surgical CPT is recorded in CaseFacts and named consistently in the operative record and every referencing document (probe: cross-document grep)
- [x] ISC-94: Subpoenaed-records provider attribution round-robins over `CaseFacts.providers` (restores the substrate `provider_index` behavior the engine path dropped) (probe: multi-packet case, distinct providers in extracted text)
- [x] ISC-95: A coherence harness sweeps ledger-vs-extracted-text for every rule above with ≥1 positive control per rule (probe: harness pytest, controls fail red when planted)
- [x] ISC-96: Anti: every byte difference vs v0.2.0 traces to a declared source — a fact-aware registry subtype or the ISC-94 provider-attribution defect fix — and a case containing neither renders byte-identical (probe: full-demo diff, 0 unexplained changes; refined 2026-07-28 — the original wording conflicted with ISC-94, whose provider fix legitimately moves SUBPOENAED_RECORDS bytes outside the registry; see Decisions)
- [x] ISC-97: Demo double-run + `TZ=Australia/Sydney` identity holds with the facts layer active (probe: tree md5 ×3)
- [x] ISC-98: Version 0.3.0 with numbered compatibility notices for byte changes on override-covered subtypes (probe: `--version` + CHANGELOG grep)
- [x] ISC-99: Resolved CaseFacts surfaced per case as `case_facts.yaml` beside the resolved seed (probe: output-dir listing + load round-trip)
- [x] ISC-100: Full suite + ruff green; no file under `merus-test-data-generator/` modified (probe: suite run + git diff empty on substrate)

## Test Strategy

| isc | type | check | threshold | tool |
|---|---|---|---|---|
| 1–10 | structural | files/commands exist and run | exact | Bash/Read |
| 11–21 | unit | Pydantic validation + seed resolution | exact | pytest |
| 12, 69 | determinism | manifest hash equality across runs | identical | Bash sha256 |
| 22–29 | unit+integration | control matrix cases | exact counts | pytest |
| 30–53 | integration | one seed per path; manifest assertions | subtype presence + date order | pytest + jq |
| 54–59 | integration | all-353 forced render + full-case text sweep | 0 fallbacks; 0 cross-case intrusions | pytest |
| 60–64 | cross-repo | set-diff vs classifier source | ∅ drift | pytest + Bash |
| 65–69 | integration | manifest schema validation | schema-valid | pytest |
| 70–71 | gate | pytest, ruff | pass | Bash |
| 72–74 | anti | denylist sweep over output; sentinel-tree diff | zero hits; zero files outside `--out` | pytest |

## Features

| name | description | satisfies | depends_on | parallelizable |
|---|---|---|---|---|
| package-scaffold | pyproject, src layout, CLI skeleton, doc quartet | 1–10 | — | yes |
| seed-schema | CaseSeed/CaseloadSpec models, loader, templater, distributions | 11–21 | package-scaffold | yes |
| doc-controls | include/exclude/override/cap/format-mix resolver | 22–29 | seed-schema | no |
| lifecycle-bridge | substrate DAG wrapper + stage mapping | 30–38 | seed-schema | yes |
| lien-machine | lien claimant tracks through resolution | 39–46 | lifecycle-bridge | no |
| recon-machine | recon round-trip + post-recon paths | 47–53 | lifecycle-bridge | no |
| renderer-bridge | sys.path bridge, registry resolution, deterministic scan seed | 54–59 | doc-controls | yes |
| taxonomy-sync | 353-subtype import + drift check + validate cmd | 60–64 | package-scaffold | yes |
| manifests | per-case + caseload manifests, provenance | 65–69 | renderer-bridge | no |
| quality | tests, ruff, example caseload | 70–74 | all | no |
| perspective | applicant vs defense file POV: cast owner, work-product swap, emission profiles | 75–84 | lifecycle-bridge, renderer-bridge | no |

## Decisions

- 2026-07-27 15:05 — New package (`wc-synthetic-caseload-engine`) rather than updating merus-test-data-generator in place, per Alex's mid-session direction "This becomes a new tool in that same repo."
- 2026-07-27 15:05 — Python 3.12 despite the TypeScript-first PAI rule: the reusable substrate (25 ReportLab template classes, content pools, lifecycle DAG, scan simulator) is Python; a TS rebuild would forfeit ~90% reuse. Surfaced to Alex as an explicit stack decision needing his ack.
- 2026-07-27 15:05 — Seed schema designed as a superset of the KB PRD `CaseParameters` (17 fields, `docs/PRD-WC-ATTORNEY-MOCK-CASELOAD.md`) so the 47-case mock caseload becomes directly expressible.
- 2026-07-27 15:05 — ISC count 74 < E4 soft floor 128 (show-your-math): criteria are already single-probe atomic; further splitting would enumerate per-subtype render checks (353 rows) adding count without information. The full-range render test (ISC-54) covers that surface as one probe.
- 2026-07-27 15:05 — Ticket created before branch per standing feedback memory; initially filed as ADJ-1818 (AdjudicaAI) reasoning from consumers.
- 2026-07-27 15:40 — Alex: "This is not an ADJ ticket." Moved to AJC (repo precedent: gbs-tools-and-resources packages ticket under AJC, e.g. AJC-20..24) → now **AJC-34**; branch renamed `ajc-34-wc-synthetic-caseload-engine`.
- 2026-07-27 15:05 — Branch based on `docs/glass-box-code-reviewer-agent` HEAD (dirty unrelated tree); PR will be re-based onto origin/main via clean cherry-pick at completion.
- 2026-07-27 15:05 — Skipped reading `Examples/canonical-isa.md` (Scaffold step 2) — twelve-section contract already loaded from skill doc this session; time-budget trade documented.
- 2026-07-27 17:10 — Phase B deviations accepted: `planner.py` + `determinism.py` as dedicated owners; `SINGLETON_SUBTYPES` (one denial letter / one C&R per case, liens/recon exempt); substrate lien/recon emissions suppressed so seed-owned machines are sole source; PYTHONHASHSEED re-exec chosen over per-site sorting (fixes the class).
- 2026-07-27 17:25 — Advisor (Rule 2) verdict: 4 blockers → accepted: substrate-SHA pin in manifest, subtype-coverage stats, clock-shifted determinism probe, synthetic-data marker (metadata-level; visible watermark needs substrate → surfaced). Letterhead provenance (substrate hardcodes "Martinez & Associates, APC") → surfaced to Alex, not fixable from this package.
- 2026-07-27 17:30 — Clock-shift probe (TZ=Australia/Sydney) CONFIRMED advisor: ~60/289 files drift (EML Date local offsets; PDF/docx timestamp normalization TZ-dependent). Same-machine determinism real; cross-TZ broken. Fix: pin all timestamp derivations to fixed zone. Advisor and empirics agree — no Rule 3 conflict.
- 2026-07-27 19:05 — refined: Alex: "I want this engine to be able to produce Applicant sided cases or defense sided cases." Defense POV moved from Out of Scope to ISC-75..84. Design: `perspective` seed field flips file ownership, work-product authorship, and emission profiles — never the case facts (ISC-82 anti-criterion guards this).
- 2026-07-27 20:10 — Perspective builder authored parallel ISC-75..84 wording in its worktree (it branched before master's criteria were committed). Master numbering wins per ID-stability; its criteria map 1:1 onto master's and all evidence carries over. Its `floor` addition to PERSPECTIVE_PROFILES (weights can't multiply zero-proposal subtypes like INVESTIGATION into existence) accepted as a design improvement; cap-vs-floor bug found and fixed by its own demo gate.
- 2026-07-27 17:35 — Forge interim: re-exec safe (os.execve, double loop-guard; `-m` form not preserved — noted), manifest md5s hash final bytes (normalize-then-hash order verified), `py.typed` declared but missing.
- 2026-07-27 18:20 — Runway validation placed at the **seed boundary** (fail loud), not as a lifecycle repair. The seed is the interface, so an impossible story is rejected where it is written; the lifecycle then only has to enforce invariants it can always satisfy. Floors keyed on three independent drivers (stage, real resolution, post-resolution litigation) because a seed answers to all three at once.
- 2026-07-27 18:20 — `CaseTimeline` validates its own ordering in `__post_init__` and raises `TimelineInvariantError` rather than asserting — asserts vanish under `python -O`, and a silently inverted spine is the exact defect being fixed.
- 2026-07-27 18:25 — Track dates are now built **unclamped** and fitted once per chain (`fit_track`), instead of clamped per document. Per-document clamping is what collapsed a five-document lien track onto one date; a two-pass isotonic fit keeps the order and only compresses the spacing.
- 2026-07-27 18:25 — Post-resolution lien tracks **extend past** the anchor rather than compress. The anchor is an artifact of determinism, not a fact about the file, and legible ordering is worth more than a document dated after "today".
- 2026-07-27 18:40 — Substrate clock pinned at runtime (`pin_substrate_clock`), a deliberate exception to the no-patching rule. Distinguished from the letterhead case (left unpatched): the anchor is a process-wide constant, not per-case mutable state. The alternative was abandoning the cross-machine determinism promise.
- 2026-07-27 18:50 — Faker deliberately **not** patched. Rebinding `faker.providers.date_time.datetime`/`dtdate` breaks the `isinstance` checks in Faker's own date parser (`ParseError: Invalid format for date`). Since `date_of_birth` is the substrate's only clock-relative Faker call, the field is owned in `case_context._date_of_birth` and derived from `seed.rng("dob")` instead.
- 2026-07-27 18:55 — Empirics beat the fix plan: the CLI-level TZ gate reported 0 drift while an in-process two-zone probe still found 3 files drifting (a DOB one day apart). The in-process regression test was kept as the sharper instrument; the CLI gate alone would have shipped the Faker leak.
- 2026-07-27 19:05 — `substrate_git_sha()` scoped to the substrate **path** (`git log -1 -- .`) rather than the briefed bare `rev-parse HEAD`. In a monorepo bare HEAD moves on every unrelated commit, so the pin would warn constantly — an alarm that always fires is an alarm nobody reads. Deviation surfaced in the handoff.
- 2026-07-27 late — **Cato remediation round.** One line per change:
  - ISC-54 rewritten to its honest scope and evidenced by `test_render_coverage.py`, which drives all 353 canonical subtypes through `render_document` directly (~25 s, kept in the default suite) rather than inferring coverage from a caseload.
  - The audit's predicted exceptions list came back with exactly three entries — `PETITION_FOR_PENALTIES`, `NOTICE_OF_PENALTY_5814`, `NOTICE_OF_PENALTY_5814_5` — and they were **fixed rather than recorded**: they are the engine's own `OVERLAY_SUBTYPES`, invented here to reach classifier parity, so `renderer.OVERLAY_TEMPLATES` now owns their dispatch (`ApplicationForAdjudication` for the petition, mirroring the substrate's own `PETITION_FOR_PENALTIES_LC_5814` entry; `CourtNotice` variants for the two notices). `KNOWN_UNRENDERABLE_SUBTYPES` ships empty with an assertion that it never grows.
  - `registry.get_template_for_subtype` returns `GenericDocumentTemplate` for unknown keys, so "no template" and "generic on purpose" were the same value; `RenderResult.template` and `.fallback` now separate them, and both reach every manifest document entry.
  - `validate --out` fails on any `fallback: true` and gained `--allow-fallback`; the report line `fallbacks : N` is always printed. The caseload manifest gained `distinctTemplates` and `fallbackCount`.
  - ISC-74 upgraded from a `git status` observation to a sentinel-tree diff: `HOME`, `TMPDIR`, `TMP`, `TEMP` and the three XDG dirs are redirected into one monitored sandbox, seeded with sentinel files, and diffed on size+mtime after a subprocess generate. Zero creations, modifications or deletions — no font-cache or temp-spool leak.
  - ISC-73 upgraded from an assertion about the generator to a sweep of its output: `tests/data/name_denylist.txt` (real CA WC carriers and defense firms, with `Martinez & Associates` explicitly ALLOWed as the substrate letterhead) is checked against extracted text from all 256 text-bearing demo documents and against every manifest cast field, with a positive control proving the probe fires.
  - **Real-entity leak found and closed at source.** The substrate's `data/wc_constants.py` pools `INSURANCE_CARRIERS` and `DEFENSE_FIRMS` are *actual* companies (State Fund, Zenith, Bradford & Barthel, Laughlin Falbo…), drawn whenever a seed does not name its own. `case_context._replace_real_organizations` substitutes coined names on that path and rebuilds the derived adjuster/defense emails, which carried the old domain even when the name was overridden. The demo's seven real carriers and two real defense firms were renamed to coined equivalents. Substrate untouched.
  - `zeroRealPii` is now computed from `CaseCast.provenance` (`faker` | `seed` | `engine`) instead of being a hardcoded `true`, with the derivation published as `provenance.castProvenance` and a test that constructs an unvouched cast to prove the flag can be false.
  - ISC-58 upgraded from a 3-document sample to a full-case sweep with a cross-contamination guard against the other six casts. **Scope correction found by the sweep:** an ADJ number does not exist before the Application for Adjudication is filed, so 10 of the 11 documents lacking it are correctly pre-filing. The assertion is keyed to the filing date; `MEDICAL_CHRONOLOGY_TIMELINE` is the single documented uncaptioned exception.
  - ISC-27 evidenced by a chi-square test over a 120-document case (α = 0.001, computed inline — SciPy is not worth a dependency for six lines), with a positive control that rejects a skewed draw, plus the same statistic applied to the shipped demo.
  - ISC-12 gained entrypoint parity: `python -m wc_caseload_engine`, `-m wc_caseload_engine.cli` and the `wc-caseload` console script produce byte-identical trees, closing Forge's note that the `-m` form is not preserved across the `PYTHONHASHSEED` re-exec.
  - Rider: `perspective` added to both shipped seed templates (`seed --template`, `--kind caseload`).
- 2026-07-27 19:20 — Harness deviation: the agent was fenced into a git worktree that predates this package, so the briefed "work in the main checkout" was impossible (Edit and git both refused shared-checkout paths). Worktree fast-forwarded to the `ajc-34` tip, the four uncommitted review fixes carried across by content, and the result committed as a cleanly cherry-pickable delta.
- 2026-07-27 night — **GPT release-review FAIL round.** One line per fix:
  - **Org-pool sweep (BLOCKER).** The substitution covered carriers and defense firms; the substrate has *four* organization pools, and `ALL_EMPLOYERS` (Safeway, Costco, Kaiser Permanente, UPS, City of Los Angeles) plus `MEDICAL_FACILITIES` were flowing straight through — a real employer named as defendant in every caption under `zeroRealPii: true`. All four are now coined with seed-stable names; the employer suffix is chosen by the substrate's own industry key so the (industry, company, position) trio stays coherent. Root bookkeeping error fixed too: `employer` was classified `faker` when it was a *pool* draw, so the flag vouched for a channel it had never checked.
  - **Denylist made one list, and the pool audit made dynamic.** `name_denylist.txt` moved from `tests/data/` into package data — the engine reads it (to warn) and the probes read it (to sweep), and two copies of a safety list is one copy plus a liability. The structural guarantee is `substrate_organization_pools()`, which reads `data/wc_constants.py` **live**, so a pool that grows upstream is swept without anyone editing a fixture; a renamed constant raises rather than silently sweeping nothing.
  - **Seed-declared names: kept, warned, never overridden.** The seed is the contract, so a name its author wrote stands; it is checked against the denylist and logs `cast.seed_name_on_denylist`, and `castProvenance` already distinguishes `seed` from `engine` so a reviewer can see whose choice it was. Both directions tested — the warning fires on `Costco Wholesale` and stays silent on a coined name.
  - **Branch runway floors (BLOCKER).** Runway was validated against stage and resolution only, so a 30-day `intake` + `denied` seed passed and then stacked its denial letter, Application and DOR all on 2026-01-01. `runway_demands()` now enumerates every demand and the error names the binding one. Numbers derived from the encoded chains, not estimated: denied 90, ur_dispute 65, imr 120, **eval 240**. The review proposed 120 for the evaluation; `_guaranteed_eval_documents` draws the panel at injury+180 and the report at +60, so 120 cannot hold the sequence — the code was believed over the estimate, and `_stage_runway_floor` + `_STAGE_AGE_DAYS` were widened to match so auto-derivation stays compliant by construction.
  - **Denial chain fitted, not clamped (BLOCKER, second half).** Denial → Application → DOR is a *sequence* — each document exists because of the one before it — so it now goes through `fit_track` like the lien and recon chains. Per-date clamping is only sound for the parallel core track; applied to a chain it pins every overrun onto the horizon, which is precisely the reproduction.
  - **PYTHONHASHSEED guard (BLOCKER).** The guard deferred to any pre-set value, reading `PYTHONHASHSEED=random` — the default spelled out longhand — as a deliberate determinism choice, and treating `1` and `2` as equivalent when they are two different salts producing two different caseloads. Only `"0"` is accepted now; everything else re-execs. `WC_CASELOAD_NO_REEXEC` still opts out, now with a warning naming the consequence, because a silent opt-out is one nobody knows they took.
  - **Recon briefing order (MAJOR).** The order was drawn independently of the briefing schedule and the chain was then sorted by `(date, subtype)`, so the sort faithfully recorded a Board ruling filed before the reply it had supposedly considered. The chain is now built in *legal* order and the dates fitted to the sequence, never the reverse; `_order_date` floors the ruling at the day after the last brief. Trade accepted and measured: constraining the order can push it past the LC 5909 sixty-day window, so the test counts how often (≤10 in 50) — a ruling a few days late is an ordinary file, one that predates the briefing is not a file at all.
  - **Bytecode containment (MAJOR).** Importing the substrate scattered 13 `__pycache__` directories through a read-only dependency's source tree — writes outside `--out`, from a tool whose contract is that there are none. `sys.dont_write_bytecode` is set on the first executable line of `__init__.py` (earliest reachable point; `cli.py`/`__main__.py` set it too for direct import paths) and `PYTHONDONTWRITEBYTECODE=1` rides through the re-exec. Substrate tree: zero. Package tree: one file, `__init__`'s own bytecode, which CPython writes while compiling the file that would set the flag. Named explicitly in the anti-probe rather than exempting a directory, because it is an interpreter floor, not a gap.
  - **Post-resolution liens: documented, not changed (DESIGN).** Tracks extending past the case-in-chief resolution under `post_resolution_litigation: true` is intended — real lien practice outlives the case, and the anchor is an artifact of determinism. The boundary is now asserted, not just described: without the flag the horizon still binds, so the extension cannot leak into ordinary cases.
  - **Caption exemption made structural (found by this round).** The recon fix shifted format assignment and exposed a latent gap: `QME_PANEL_REQUEST_FORM_105` renders through the substrate's `ClientIntake` letter template, which prints no case caption for any of the ~60 subtypes routed to it. Exempting by *template* rather than by subtype names the cause instead of enumerating symptoms, and a companion test caps the exemption at a third of the file so it can never grow large enough to hide a regression.
- 2026-07-27 23:15: refined: ISA reopened at iteration 2 on Alex's directive to run the ISC-21 KB-grounding follow-up. ISC-21 split per ID-stability into ISC-21.1..21.6 (content table completeness, taxonomy-canonical targets, plan-level content flags, per-hook rendered-language probe, hook-free anti-probe, manifest surfacing + determinism); progress recounted 83/90.
- 2026-07-27 23:15: EnterPlanMode skipped at E3 (show-your-math): the criterion, its deferral note, and the follow-up scope were already user-approved in this ISA; the current directive is an explicit "run it" on that scope.
- 2026-07-27 23:15: Injection architecture: engine-side dynamic subclass of the resolved substrate template overriding `build_story` — append-only doctrine ADDENDUM/AUTHORITIES section, paragraphs drawn via `derive_seed(rng_seed, "doctrine:{index}:{hook}")`, never the global stream, so the pre-existing story draw sequence and hook-free byte-stability are untouched; empty `content_flags` takes the exact original code path. Substrate stays read-only per constraint.
- 2026-07-27 23:55: Pre-existing coupling pinned, not patched: `seed_to_case_parameters` flips substrate complexity to `complex` at ≥2 doctrine hooks, changing clinical content in every document of the case including untargeted ones. Predates this ticket. `test_only_the_hook_count_changes_an_untargeted_document` now documents it so the next reader meets it as behavior, not as a bug.
- 2026-07-28 00:10 — **Third cross-model review round (full-branch + doctrine addendum): FAIL, remediated in 2e35cc9.** 19 findings; the six blocking ones fixed same-session: (1) ci-gate omitted the package from `needs`/`results` — its CI was non-blocking from day one; (2) `zeroRealPii: true` was asserted over a *detected then discarded* denylist hit — now `PROVENANCE_SEED_DENYLISTED` (outside `SYNTHETIC_PROVENANCE`) flips it false and the hit reaches manifest warnings; (3) non-canonical override subtypes reached manifests via `generate` — `normalize_control_keys` maps or rejects at plan build, fail-closed; (4) lien/recon manifest summaries described the machines' pre-control proposals — now `documentCount` = emitted, `plannedDocumentCount` = proposal; (5) the TZ format-coverage guard asserted only eml — now all four formats, both runs; (A1) doctrine paragraphs asserted facts the seed cannot represent (Benson "two dates of injury" on a one-`InjurySpec` schema) — 13 paragraphs rewritten to contention framing + per-hook `DoctrinePrerequisite` gating auto-derivation, explicit incoherent hooks kept-but-warned per ISC-29; (A2) `RenderResult.content_flags` could claim doctrine not in the bytes — canonicalized through `content_flags_for` before wrapping. Findings 6–19 deferred to **AJC-35**. Review-round collateral: a pre-existing anti-probe test had finding (2)'s bug written into its assertion, and `test_perspective.py` seeded a real CA defense firm (denylist hit) — both corrected.
- 2026-07-28 00:10: Scope call (flagged): Benson's prerequisite is two impaired *regions*, not two *injuries* — `CaseSeed` models exactly one `InjurySpec`, so the faithful predicate would delete the hook. Documented in code + README; a second date-of-injury is a seed-schema change, deferred. Demo caseload deliberately left as-is: nguyen (benson) and ramirez (gfpa) explicitly seed incoherent hooks and now surface the designed warnings.
- 2026-07-28 04:30 — **Iteration 3 opened: scenario seed v2, Phase 1 (AJC-37, Alex "go").** Ground truth from the full mechanical survey: clinical content is per-template global-random draws with no case ledger (a QME can cite an MRI no diagnostic report shows), the substrate's `CaseContextAccumulator` is starved and never wired on the engine path, `has_surgery` gates documents but zero content, and subpoena packets are all mis-attributed to the treating physician (engine never sets `provider_index`). Architecture: engine-owned `CaseFacts` ledger derived at plan time + a fact-aware template-override registry in the renderer (subclass per flagship subtype — the doctrine-injection seam generalized from append-only flowables to method-level overrides), substrate untouched. Phase 1 = ledger + registry + flagship trio (diagnostic report, QME imaging citations, surgery language) + provider round-robin restore + coherence harness with positive controls. ISC-85..100. Personas (adjuster/attorney), treatment trajectories beyond surgery/diagnostics, and discovery volume land in later phases.
- 2026-07-29 03:30: refined: Phase 3 split at the limit boundary — 3a (adjuster persona, penalty re-gating, IGNORECASE audit reconciliation, declared-vs-resolved class killer) lands now; 3b (attorney cadence effects ISC-123..125, letter coherence ISC-119/121, discovery unification ISC-126, message registry ISC-129) is the next branch. Driven by API-limit pressure + the small-PR discipline; the 3a increment is coherent and fully gated on its own.
- 2026-07-29 04:20 — **PR #25 remediation round + merge (9dc0d6a; merged to main as d34c744).** Review round 1 on ce950e3: 2 MAJOR / 3 minor. All five closed red-first, each a mechanism fix: (M1) the LC 5814 penalty petition was appended AFTER `resolve_document_controls` ran — an explicit `documents.exclude` naming it was silently ignored; it now enters the resolver as an ordinary candidate, and the precedence decision is *control wins, loudly*: an explicit exclude/include_only suppressing a ledger-earned petition lands an actionable manifest warning (opposite-draw control: attentive+exclude warns nothing). (M2) three P3 schema docstrings claimed consumption that does not exist (`pages_per_set`, `subpoena_sets`, cadence effects) — inverted to "accepted and validated, not yet honoured … changes nothing about the output", which is falsifiable prose. (m3) vacuous test renamed to what it actually checks. (m4) `derive_case_facts` ran twice per case with divergent results (cast built after the first call) — one cast-bearing derivation now serves plan + manifest, spy-verified at 1 call/case; **this was the whole 573s→147s runtime regression flagged as the first 3b item — closed**. Also memoized `_substrate_modality_hits`. (m5) `diligence` was published in `caseFacts` but no document renders it — the governed-fields rule applied to its own author: unpublished (kept `lateBenefitEvents`/`maxDaysLate`, both recoverable from rendered notice dates), 9 dependent tests fixed rather than the rule argued down. (v1) relayed codex claim verified CORRECT: `min()`+`>=` could file the petition between late notices — now `> max(actual_date)` asserted across 40 seeds (checked≥10 floor), clamp hardened.
- 2026-07-29 04:20 — **Confirmation review: pass; approved and merged.** Reviewer (GPT-5.6-Sol, high effort) re-proved every closure by execution: independent suite run 741 passed at 2.1× speedup; decisive M1 membership test (`include_only: [PETITION_FOR_PENALTIES]` alone yields exactly the petition — distinguishes a real resolver path from a bolted-on special case; `global_cap` also binds it); no-noise warning counterfactual; derivation spy 1 call; independent 120-seed dating sweep 0 violations (3× wider than shipped). Two MINOR residuals, both *disclosure* gaps not correctness (control is honoured; the explanation is missing) → ISC-135/136; reviewer's meta-guard proposal ("not yet honoured" marker → byte-inertia assertion, closes the code-vs-prose class the implementer flagged as unguarded) → ISC-137. One codex claim rejected on the recoverable-from-documents rule (publishing `lateBenefitEvents`/`maxDaysLate` is proper: the notice dates ARE rendered — "a template renders the value" ≠ "the fact is checkable from the documents"). LexPerspex approval 4807420933 (basis: the completed two-model cycle); CI green (CI Gate + package job; 16 packages path-skipped); merged as d34c744. Phase 3a CLOSED at 137/147; phase → execute for 3b.
- 2026-07-29 13:40 — **Phase 3b build record (d82a1be..f0a491e).** ISC-137 built FIRST by design, and it earned the position twice over. (1) It fired mid-phase on its own: when cadence became honoured, the guard failed the build until the "not yet honoured" docstring flipped, the stale probe died, and `attorney.cadence` + `adjuster.letterTypesAllowed` entered the governed-facts set — the code-vs-prose class converted from a review catch to a red test inside the same PR that created it. (2) **Latent Phase-2 defect found and fixed (71710f0/f0a491e):** the substrate threads `doc_spec` to `build_story` as a parameter and never assigns `self.doc_spec`, so every engine helper reading `template.doc_spec.context` silently got its default — `_facts_of` escaped via the renderer's `_wc_case_facts` instance attribute, but `_report_ordinal`/`_index_of` had no second route, so **every report rendered at ordinal 0 since v0.4.0 shipped**. Phase 2's trajectory test passed for the wrong reason (`any(phrase in body for phrase in arc)` — at ordinal 0 the first arc phrase IS an arc phrase): the vacuous-assertion class, one more instance. Fixed by `_SpecCapture` binding the spec in `generate()`; substrate untouched; demo blast radius exactly 1 PR2 (the only demo case with two PR2s — its second now renders ordinal 1). (3) ISC-119 delay chains ride `DEMAND_LETTER_FORMAL` (applicant counsel chasing the late benefit), one per late event, emitted as candidates BEFORE `resolve_document_controls` — the PR #25 M1 lesson applied at design time. (4) ISC-124's first cut was wrong and its counterfactual missed it (milestone anchoring left one anchor → a 90-day metronome that still differed from the other cadences); re-anchored to documents in the file (8 anchors, letters 1–5 days behind real ones). Recorded as a counterfactual-design lesson: differing from siblings is not the property; the property is the anchor relationship.
- 2026-07-29 13:40: refined: guard-narrowing reviewed and accepted (orchestrator eyes + flagged for cross-model review): `test_neither_output_carries_an_ungoverned_field` moved from substring sweep to a key walk because `letterTypesAllowed`'s VALUE `pd_advance_offer` contains banned NAME `pd` — every banned name is a field name, so keys are the real rule; the planted-leak control (`test_the_key_walk_still_catches_a_planted_leak`) proves all five banned names still caught, and the docstring names the guard-death risk explicitly.
- 2026-07-29 13:40: refined: Phase 3b ships at 7/10 ISCs — 125 (rendered anchor references), 126 (discovery unification), 129 (message registry) split to **Phase 3c**, same coherent-increment rule that split 3a/3b. ISC-126 diagnosis recorded for the 3c implementer: ToC totals sum per-record-type `random.randint` at `subpoenaed_records.py:163-186` while body pages draw independently at 264+; `Table._cellvalues` verified mutable post-construction (unlike `Paragraph.text`) — a viable unification seam. `subpoena_sets`/`pages_per_set` keep their "not yet honoured" markers and ISC-137 fails the build if that stops being true.
- 2026-07-29 13:40: KNOWN (numbered notice, not evidence): the demo caseload proves the letter lifecycle and trajectory fix but proves NOTHING about cadence or delay chains — no demo case draws a late benefit or carries enough client letters to have a rhythm. An unearned zero, recorded as compatibility notice 2. Phase 3c should consider a demo seed exercising negligent+cadence so the flagship examples witness the personas.
- 2026-07-29 14:15 — **PR #26 review: pass-with-concerns; approved and merged (80f0606).** Reviewer (GPT-5.6-Sol, high effort) re-executed the suite (786 passed) and proved by execution: both PR #25 residual paths now warn — closed by the GENERAL fix (outcome-keyed warning covers the unprobed parent-type/perspective paths too, "the general form rather than two more special cases"); delay-chain candidates respect exclude/include_only/caps — the M1 lesson propagated FORWARD to unreviewed code for the first time in the series; ordinal-fix blast radius reconciles exactly (19+1+1=21); guard narrowing attacked and accepted as more precise, not weaker (codex's `{"fieldName":"wpi"}` evasion shape requires a banned NAME as a VALUE — not the leak shape the guard exists for). Two MINOR proof-tightness findings, no behaviour change, both mandated as the FIRST Phase 3c commits: (F1) ISC-137's discovery-field inertia probes run at `target_stage: medical_legal`, which emits ZERO subpoenaed-records documents — inertia proven where the consumer is absent, i.e. our own "a zero that is not earned is not evidence" standard violated by our own new guard; fix = point the probes at discovery/pre_trial/resolved (reviewer measured 2 packets each) BEFORE ISC-126 makes the guard load-bearing. (F2) CHANGELOG claims a 1–5-day anchor window; the named test asserts 0–60 — prose tighter than guard, the code-vs-prose class ONE LEVEL UP from docstrings (CHANGELOG/README claims have no meta-guard; epistemics thread stays open). LexPerspex approval 4809010209; CI 18 checks green.
- 2026-07-29 14:15 — **Phase 3c opened** (branch ajc-37-scenario-p3c from 80f0606). Scope: F1 + F2 probe repairs first, then ISC-125 (rendered anchor references), ISC-126 (discovery unification — diagnosis recorded 13:40: ToC randint sums at subpoenaed_records.py:163-186 vs independent body draws at 264+; `Table._cellvalues` mutable post-construction is the seam; on wiring, the ISC-137 markers MUST flip and the repaired inertia probes MUST start failing red before the flip — that is F1 paying off), ISC-129 (table-driven message registry), and a demo-caseload decision on the unearned zero (a negligent+cadence demo seed would witness the personas; it moves demo bytes intentionally — weigh as its own numbered notice).
- 2026-07-29 18:05 — **Phase 3c build record (ce5d215..451fa80, two implementers).** Implementer A completed F1/F2/ISC-125/ISC-126 then STOPPED at low context rather than half-build ISC-129 — the half-built-guard discipline applied to one's own capacity; Implementer B (fresh context) took the branch from b8703c6. (F1) inertness probes rebuilt as `InertProbe` carrying the lifecycle where the consumer lives PLUS witness subtypes, guard-checked against each other; first red-capability control was itself bad (unconditional append — the OLD blind probe caught it too, proving nothing) and was reshaped to the real form: old 57→57 passes blind, new 74→78 catches. Reviewer's "2 packets" corrected to a measured 3. (F2) measured 404 letters: 82% at exactly 5d, 0 unanchored — "1–5" was false at both ends; two intermediate wrong fixes left visible (fixed 50 from a 38-seed sample; a 12-seed sample at discovery immediately produced 100); final bound derived per case, threshold named `CADENCE_MIN_LETTERS` read by guard and test both. (ISC-125) rendered letters name their anchoring document; cited dates parsed back OUT of the PDFs and checked against the manifest — naming *an* event is not the property, naming a *real* one is; 6 references event-driven, 0 in the every_30_days control. (ISC-126) ledger = cover-sheet ToC = physical paper, one draw path; needed RNG-state restore per attempt, walk-down-to-fit + pad-up (sections atomic, overshoot unrepairable), fresh substrate content for growth (flowable reuse both crashes and would duplicate visits); the mandated sequencing paid off exactly — both F1-repaired probes went RED at the WIP commit and green only at completion; at medical_legal they would have reported inertia and this could have shipped hollow. `INERT_PROBES` now empty = the intended end state; every field honoured, each removed only after its inertness run went red. Third bad-probe near-finding this phase: packet 4 read DISAGREE until the probe looked past page 1 (cover table overflows) — the probe was wrong, not the packet.
- 2026-07-29 18:05 — **ISC-129 registry design.** `message_audit.py` AST-parses seeds.py, resolves one level of helper indirection, classifies instructs-vs-reports by clause-initial imperative verb: 57 messages, 11 actionable, 0 unresolved raise sites. Entry identity = directive clause tuple + raise site (reword the advice → stale AND missing both go red; fix an unrelated comma → no churn). Red-first: empty registry named all 11. Six mutation classes proven red (wrong trigger, non-resolving edit, reworded directive, deleted entry, wrong where, blinded detector); the planted control runs the SAME unregistered() against doctored source, not a second implementation. `short_runway`'s entry parses the message's own stated date back out and loads a seed at exactly that boundary (the declared-vs-computed class); the vocabulary limit is itself executable ("Nudge it upwards" invisible, "Raise it upwards" seen). Three hand-written p2 proofs deleted as strictly subsumed. **The guard caught its own author**: the first message_audit.py docstring quoted the dead `wc-caseload taxonomy --list` invocation while explaining why dead invocations are dangerous — the CLI sweep failed it; prose fixed, guard untouched, recorded as compatibility notice 5. That is the meta-guard demonstrating liveness on its own maintainer, twice in one phase.
- 2026-07-29 18:05 — **decision: NO persona case added to demo-caseload.yaml** (orchestrator ruling on the implementer's measured recommendation). The unearned zero is real (7/7 demo cases: lateBenefitEvents=0; one client letter total, below CADENCE_MIN_LETTERS) but examples/personas-showcase.yaml (PR #27, merged) witnesses everything: regenerating it on HEAD moves 34/391 documents, all traced (17 ISC-125 letters + 17 ISC-126 packets), with the two non-event-driven cadence cases moving ZERO letters — a natural control. A probe eighth demo case measured purely ADDITIVE (331→386, 0 changed, warnings still 2) so the earlier "moves demo bytes" concern was too broad — but it fails to witness ISC-120 as shaped (4 late events, 0 petitions), would churn five 331-count prose pins, and the demo deliberately carries no scenario block: it is the byte-accounting baseline every phase diffs against. Separate concerns, separate files — the doctrine-showcase precedent. The zero stays, now DOCUMENTED as covered-elsewhere rather than unearned.
- 2026-07-29 18:40 — **PR #28 remediation (0195f43, docs+tests only, demo digest unchanged).** Review round 1: pass-with-concerns; one MINOR (F1 witness assertion executed zero times with INERT_PROBES empty — red-capability by reading, the class F1 replaced) + six codex prose-drift candidates relayed unverified. All six MEASURED by the implementer: (a) FALSE — user-guide claimed diligence published (fixed, with the unpublication reason stated in place); (b) TRUE-and-that-is-the-defect — `resolve_document_controls` runs before `_shape_discovery`, so an exclude/zero-override on packet subtypes is silently REFILLED to the requested count (exclude → still 4 packets, no warning), and the strip-everything warning blames the lifecycle stage, not the control → **ISC-148**; (c) TRUE in range, FALSE outside — 6/27 packets across budgets 1–120 broke ledger=paper (structural floor near 4 pages) with `packet_page_table_unstable` firing 0 times → **ISC-149**; honest-statement route chosen over an end-of-session fix on the flagship claim, explicitly citing how the ordinal defect shipped; (d) TRUE, qualified — 17/17 event-driven letters cite anchors, the no-anchor fallback branch real but unexercised in 412 letters/80 cases; (e) FALSE — never_treated suppresses 3 parent types minus a 5-subtype allowlist (11 suppressed / 36 retained measured), prose now enumerates; (f) UNDERSTATED — the detector hole is displaced verbs, not unknown ones ("You should set…" evades); disclosure widened + `test_no_shipped_message_falls_into_the_hole` turns it into a guard whose first draft FAILED on a noun-reading of "seed" (false positive recorded in-comment) → **ISC-150**. Item 1: planted InertProbe control extracted so the control runs the real `assert_probe_stands_on_ground` (witness OPERATIVE_HOSPITAL_RECORDS, asserted canonical first so a typo cannot pass it), paired with a passes-on-valid twin. Gates: 849+2skip, ruff clean, demo digest 42fa4d6d IDENTICAL to pre-remediation (docs+tests-only proven), byte accounting unchanged (325/6/0), warnings 2, showcase 391/0/0. Reviewer's Phase 4 charter carry-forwards recorded: behavioural-claim registry for README/user-guide/CHANGELOG (extend the ISC-129 mechanism), planted-control-on-empty-table convention, per-section ToC attribution (three-readings one level down).
- 2026-07-29 03:30: refined: ISC-96 rewritten — its original wording ("no registry subtypes → byte-identical") conflicted with ISC-94, whose provider-attribution fix legitimately moves SUBPOENAED_RECORDS bytes outside the registry. Making a substrate-defect repair opt-in to preserve a criterion's literal wording would invert the point of the criterion; the anti-property that matters is zero UNEXPLAINED drift. Full-demo evidence: 303/331 identical, 28 changed = 23 registry-covered + 5 ISC-94, 0 unaccounted.
- 2026-07-28 06:10: refined: ISC-90's "modality/body part" narrowed to modality — the substrate diagnostic report prints all injury body parts joined and never attributes a study to one region, so per-study body-part attribution is unassertable against current output (unsatisfiable, not unbuilt). Rejoins the criterion in Phase 2 with the diagnostic-report header work.
- 2026-07-28 06:10 — **Phase 1 build record (d98be53 + db944f8).** `CaseFacts` ledger on namespaced RNG streams; `scenario.{diagnostics,surgery}` Phase-1 seed subset; 9-subtype fact-aware override registry (dispatch-level subclasses; DiagnosticReport governed by draw-interception rather than a ~100-line method fork — pre-authorized narrowing); QME neuro-exam EMG leak closed by one-method override; provider round-robin restored (a substrate context key the engine path never set — every subpoena packet had been attributed to the treating physician); operative-record CPT pinned to the ledger via the substrate's own body-part-coherent pool. Two probe-methodology corrections logged by the builder itself: an early "zero uncovered moved" claim corrected to the true 303/28, and a false ×3-determinism DRIFT verdict traced to hashing `md5sum` output that embedded absolute paths. 587 tests (+32 coherence, each rule with a red-proven positive control), ruff clean, v0.3.0 with 5 numbered compatibility notices.
- 2026-07-28 07:40 — **PR #23 review remediation (cdb406b).** The review's critical was real and structural: `scenario.surgery` never reached `has_surgery` — the lifecycle flipped an independent coin, invisible to a 32-test harness sharing one `rng_seed` whose coin agreed. Fixed by resolving surgery once (scenario beats coin; coin still drawn-and-discarded so unrelated bytes never move) with opposite-coin fixtures pinned against rot; the seed-monoculture lesson is now a written harness rule. Ledger publication restricted to `GOVERNED_LEDGER_FIELDS` (one entry per modality — the exact claim the documents make); EMG excluded from the imaging path with labs' comment discipline; `validate --out` now requires, parses, and cross-checks the ledger (8 rules, all red-first). refined: ISC-92 split — the validator's forward direction ("performed ⇒ operative doc emitted") is not a property of the substrate's probabilistic emission rules (two demo cases resolve surgery true and emit none); reverse direction enforced, forward deferred as ISC-92.1 rather than making `validate` red on our own examples. 615 tests, determinism ×3, byte accounting unchanged (303/28, 0 unexplained).
- 2026-07-28 00:50 — **Fourth and fifth review rounds: the A1 class hunted to extinction.** Round 4 (a7a14be) closed the six re-review items: psych prerequisite now requires a psyche body part (a too-weak gate is worse than none — it launders incoherence as approved); the same weak-gate class found and fixed in `kite` (two impaired regions ARE seed-expressible, so gated rather than documented); post-canonicalization alias-collision checks; recon dates gated on emission (`plannedPetitionDate`/`plannedOrderDate` preserved; `awardDate` deliberately ungated — it derives from `CaseTimeline`, not a recon document); Benson legal prose softened; demo warning-set pinned as an exact-set test. Round 5 (da565d3) answered the reviewer's consistency fail with a systematic 84-paragraph sweep of all 14 hooks against their gates — principle: *the hook establishes the doctrine is at issue, never the underlying case facts* — finding TEN violating paragraphs across SIX hooks (vs the reviewer's two confirmed): going_and_coming, sibtf, gfpa, firefighter_presumption (asserted a cancer diagnosis and its date), imr_constitutionality (asserted "upheld" though `imr_outcome` may be overturned), ab5_dynamex, lc4664_prior_award. All ten rewritten to contention register and shipped as red-proven `BANNED_ASSERTIONS` positive controls; 14/14 hooks now assert only gate-established facts; README scopes the four remaining gate approximations (benson, sibtf, lc4664, firefighter) to the gate with prose guaranteed assertion-free; "cannot reach" corrected to "cannot be auto-drawn" throughout. 523 tests, ruff clean. Demo tree byte-identical to round 4 — the demo's seed draws never select the rewritten slots (only lc4664/gfpa among rewritten hooks appear in the demo); the pool-level banned-phrase sweep, not demo bytes, is the probe of record for prose.
- 2026-07-28 01:00 — **Cross-model verdict: PASS, PR #20 approval recommended.** Reviewer independently re-swept all 84 paragraphs (concurring on the four "legitimately clean" suspects), verified all ten banned phrases as real positive controls via AST-resolved literal concatenation after debugging its own first-pass test artifact, confirmed marker integrity across every paragraph (the grep corpus contract the rewrites could have broken), and explicitly declined to relay a stricter standard that would forbid first-person clinical narration and delete the feature. One non-blocking nit deferred to AJC-35: README L396-397 "no paragraph anywhere asserts a fact its own gate does not establish" should be scoped to "the doctrinal predicate its gate approximates".
- 2026-07-28 — second release-review round: eval/UR chains through fit_track, sentinel requires hashseed=0, industry-first coining. The first round fixed each defect where it was *found*; this round fixed the two it had left one level up. (a) The branch runway floors reject a seed too short for its chain but say nothing about a seed sitting **on** the floor, and the eval and UR/IMR chains were still clamped per date — so boundary-valid seeds collapsed panel request/order/report and RFA/decision/denial onto 2026-01-01 (84 of 90 boundary seeds). Both now build unclamped and go through `fit_track` in legal order, the same treatment the denial, lien and recon chains already had; `MEDICAL_TREATMENT_DENIAL_UR` also stopped sharing the UR decision's date outright (LC 4610(g)(3)(A) gives it two working days). (b) `ensure_stable_hashing` read the re-exec sentinel **before** `PYTHONHASHSEED`, so pre-setting `WC_CASELOAD_HASH_PINNED` reinstated the exact bypass the value check had just closed. Order was the whole fix: the seed is checked first and the sentinel is demoted from certificate to hop counter, capped at `MAX_REEXEC_HOPS=2` with a hard error past it rather than an exec loop. (c) `profile.employer.industry` was applied *after* the coining sweep and the position draw, so a seed naming an industry got a company suffix and a job title from the substrate's pre-override industry — `rng_seed=2` + `healthcare` produced a construction name, a healthcare department and a construction title. The industry is now applied first and the position re-drawn from the seeded industry's own titles, read live from `EMPLOYER_TEMPLATES`; a seeded `applicant.occupation` still outranks it.
- 2026-07-30 — **Iteration 4 opened: money layer Wave 1 (AJC-43).** Keystone schema for the whole money program; AJC-44 (rating), AJC-45 (defense lens), AJC-46 (applicant lens) and AJC-47 (billing) all attach to what is defined here, so field naming and boundaries carried more weight than usual. ISC-151..169.
- 2026-07-30 — **One gate for the whole layer, and it is `scenario.wages`.** `benefits` and `settlement` both require it. A settlement gross does not strictly need an average weekly wage, so a looser gate was available — rejected, because one gate makes "a seed with no wage block produces zero artifacts of this layer, and moves no output byte" a single checkable sentence instead of three, and the anti-criterion is the load-bearing property of this ticket. `wages` is `Optional`-with-`None` rather than a default-constructed block for the same reason: a present-empty block and an absent one would be indistinguishable, and the whole guarantee rests on telling "the author asked for wage facts" from "the author said nothing".
- 2026-07-30 — **`MoneyFacts` is separate from `CaseFacts`, not folded in.** The clinical ledger is derived for every case; the money spine only for a seed that asked. Keeping them apart is what lets the absence be one `None` that the planner, the renderer and the manifest each short-circuit on. Folded in, the absence would have to be spelled as a dozen `None` fields on an always-present model and every consumer would have to agree on which of them means "no money".
- 2026-07-30 — **Method names are engine labels, not statutory citations.** `actual_weekly_earnings`, `irregular_earnings_average`, `short_history_projection`, `concurrent_aggregate`, `earning_capacity`. Naming them after statutory subdivisions was considered and rejected: the label is what the analyzer (AJC-38) is scored against, so it has to mean what this package's code says it means, and a subdivision nobody here has confirmed would be an unverified legal assertion baked into the eval vocabulary. The controlling authority travels separately, as counsel-unconfirmed prose on `RateBasis`.
- 2026-07-30 — **`irregular_earnings_average` and `actual_weekly_earnings` compute identically, on purpose.** The two methods differ in the *argument* for using them, not in the sum. Inventing an arithmetic difference so the numbers looked distinct would make the label unrecoverable from the paper for the wrong reason. What the corpus carries is the recorded label plus the spread of the underlying periods, which is the real distinction.
- 2026-07-30 — **`earning_capacity` is never derived.** The catch-all is a legal argument a human makes when no arithmetic over the history fits; an engine reaching for it unprompted would be asserting a conclusion rather than computing one. Reachable only by an author naming both the method and the figure. Swept over six shape combinations rather than asserted on one seed.
- 2026-07-30 — **Which bound bound is recorded, not just the rate.** `tdBound`/`pdBound` ∈ {max, min, unbounded}. A capped rate is the same number for every high earner in a vintage, so an analyzer recovering the number without the cap has learnt nothing about the wage behind it — the cap is the fact, and an unrecorded fact is not an eval label.
- 2026-07-30 — **The rate table ships, and ships marked.** A table was needed for a seed to render a coherent document without restating the law; the risk is that a placeholder reads as settled. Answered three ways at once: the constant is named `UNCONFIRMED_RATE_TABLE`, every row carries `counsel_confirmed=False`, and every row says `COUNSEL-UNCONFIRMED` in its own `authority` text so a reader copying the citation out of a manifest cannot lose the caveat. The flag is published on every case. The *date brackets* are flagged as unverified too — a vintage boundary is a legal question and getting it wrong is as consequential as getting the number wrong.
- 2026-07-30 — **The KB-167 seam is a function signature.** `rate_basis_for(doi) -> RateBasis`, the only reader of the table, no network, no configuration hook, no import of an authority package. Deliberately the smallest possible contract: this ticket takes no dependency on work running in another repository, and that work needs no knowledge of this package beyond the shape it returns.
- 2026-07-30 — **Money lateness does NOT feed the existing `late_benefit_events` list.** The LC 5814 petition gate reads that list, so extending it would change penalty-petition emission and move bytes on cases that never asked for a money layer. Recorded separately on the benefit ledger instead; Wave 3 wires the two together. The persona is still shared — diligence is resolved once and passed in — so the file cannot describe an attentive administrator who never paid anybody.
- 2026-07-30 — **The settlement gross is pinned by ONE intercepted draw.** The substrate computes `gross_settlement = random.randint(25000, 150000)` and derives the attorney fee, costs, set-aside and net from it in its own code. Forcing only the base leaves that arithmetic internally consistent and unforked; forcing the derived figures too would be writing the disbursement, which is AJC-46's work. Matched by argument bounds so it is distinguishable from the four other `randint` calls in the same method, and a miss is logged rather than swallowed.
- 2026-07-30 — **The wage statement's tables are replaced, not the method forked.** Located by their own header text rather than by position, so a substrate edit that moves them is a miss the engine *reports* instead of silently writing over the wrong table. The letterhead, employee block, styling, HR signature and attestation stay the substrate's — only the numbers were wrong.
- 2026-07-30 — **The money floor is a floor, not an emission.** A subtype the lifecycle walk already proposed is left alone. Measured rather than assumed: the walk already proposes the wage statement at every stage and the TD payment record from `active_treatment` on, so the floor fires at `intake` and is a no-op elsewhere — which is also the probe that proves it is not dead code.
- 2026-07-30 — **`decimal`'s context is shared mutable state, and this package is a library.** Found by a probe that varied `getcontext().prec` across derivations: at `prec=6` every quantize raised `InvalidOperation`. Pinned per operation with `localcontext` rather than globally, because setting the global context would make this module the shared mutable state instead of curing it. Same class as the `PYTHONHASHSEED` and `date.today()` leaks this engine has already paid for: correct on the machine that wrote it, invisible to a same-process double run.
- 2026-07-30 — **Mutation testing found a weak guard and it was widened, not excused.** Removing `money()`'s own decimal pin left the context test GREEN, because `derive_money_facts` wraps its whole body and the outer context covered the inner calls. A guard that cannot see a defect in the code it names is not guarding it, so a direct probe on the two *public* entry points was added. Two further pins (`money_manifest_block`, `_dollars`) survive individual mutation because they are mutually redundant; removing BOTH goes red, which is the honest description and is recorded rather than papered over.
- 2026-07-30 — **PD is left alone.** `case_facts.py:915-916` (`wpi = rng.randint(3, 24)`; `pd = min(100, int(wpi * 1.4))`) is untouched and the rate derivation takes no dependency on either field, so AJC-44 inherits a clean surface. ISC-169 asserts it.
- 2026-07-30 — **Concurrent employment is bounded rather than approximated, and the bound is the honest one.** Three denominators were available for an unequal concurrent history and two of them are defensible-looking and wrong. Rather than pick one, the shape is refused: `concurrent` is a boolean, so the engine cannot say which employer a period belongs to, and it cannot compute per-employment averages without that. Wave 2 adds employer identity as an *additive* field and the shape opens properly. This is the same call as `earning_capacity` never being auto-derived — where the engine has no basis for an answer, it declines to invent one.
- 2026-07-31 — **Round 6: my own remediation shipped two documents that contradicted the facts they carried.** Round 5 chose `ORDER_APPROVING_SETTLEMENT` and `BENEFIT_PAYMENT_LEDGER` as carriers on the strength of their *names* and their *dates*, and never read what the substrate actually renders for them: generic litigation minutes saying no settlement was reached, and a provider's medical bill. The lesson is narrower than "check your work": a document is a carrier only if its **body** supports the fact, and subtype name plus document date are both necessary and neither is sufficient. The validator can check the name and the date; only a render probe can check the body, which is why ISC-212 and ISC-213 are asserted against extracted text rather than against the registry.
- 2026-07-31 — **A fatal claim is refused rather than modelled, and that is a scope decision, not an oversight.** Dependency benefits under LC 4700 et seq. are a distinct benefit class — different beneficiaries, different computation, different documents — and approximating them with temporary and permanent disability produced a first payment three days after the worker died. Wave 1 refuses the combination with a followable message and records the gap. Whoever builds the dependency ontology replaces `CaseSeed._a_fatal_injury_has_no_disability_benefits_to_pay`; until then the corpus contains no fatal case with a money layer, which is honest and is checkable.
- 2026-07-31 — **Round 10: the most valuable finding of the round was one the reviewer did not make.** ISC-232 — a false fifteen percent fee on the *release*, live on shipped showcase cases — was found while reproducing ISC-231, not reported. The reviewer's finding was a $21 edge case; reproducing it meant reading the release's arithmetic, and the arithmetic was wrong at $32,668 too. Recorded because it is the concrete payoff of the standing rule to reproduce every finding before acting on it: the reproduction is an independent look at the code, and it sees things the report does not name.
- 2026-07-31 — **The mutant-to-test mapping is itself a claim, and it has now failed twice.** Round 9 had three mutants pointed at tests that did not cover their fix; round 10 had three more. In every case the fix was real and the *guard* was the thing missing, which is exactly what a mutation campaign exists to reveal — but only if the campaign is run and re-run rather than written and believed. No round of this review has been clean on first pass.
- 2026-07-31 — **Round 9: three of my five own mutants survived first pass, and the cause is worth more than the fixes.** I wrote the mutants by naming the test I *believed* covered each fix, and for three of five that test did not touch it — one pointed at a registry meta-test, one at a whole class. The mutation campaign is only a check on the guards if the mutant→test mapping is itself checked, and the thing that checks it is running the campaign before believing the round is done. Every round has now had at least one survivor or stale mutant; not one of them was a false alarm.
- 2026-07-31 — **Round 9's findings were all "right about the term, silent about the dependent".** ISC-227 moved the approval and not the funding date derived from it. ISC-228 set a floor in the template and not in the schema that feeds it. ISC-229 owned the award and not the fee the substrate computes from it. Together with round 8's "bound applied to the wrong term", this is the same defect family seen from the other side: round 8 constrained a value adjacent to the wrong one, round 9 constrained the right one and left its dependents behind. Both are what a declaration of *what depends on what* would make structural rather than remembered — which is the causal-predecessor part of the Wave 2 entry, and this is the second independent argument for it.
- 2026-07-31 — **Round 8: both findings were a bound applied to the wrong term, which is worth naming as its own habit.** ISC-224 floored the instrument when the constraint belonged on the approval — the instrument is derived *from* the approval, so bounding the derived value let the source stay impossible. ISC-225 read the substrate's `randint` bounds as the domain of the answer when they are only the selector for which draw to intercept. In both cases the code was checking something true about a value adjacent to the one that was actually wrong. It is a cheap mistake to make and an expensive one to see, because the check is real and passes for the thing it names.
- 2026-07-31 — **Round 7: a probe that cannot tell a number from a prefix of a longer number is not reading the number.** ISC-220 shipped a corrupted figure through a green test, because the assertion was `published_value in extracted_text`. Every render probe in this layer used that shape. The ones that matter are now parsers — regex-captured, `Decimal`-compared, with a lookahead that rejects a digit or a point-then-digit after the cents — and the difference is not stylistic: substring containment cannot distinguish "the page says 1001.23" from "the page says 1001.23.20", which is precisely the defect it was there to catch.
- 2026-07-31 — **Round 7: pinning one node of a chain is not ordering the chain.** ISC-215 was right that the approving order must be dated *on* its approval, and implementing it created ISC-222 by satisfying that relation while stranding the instrument 677 days later. Two correct local rules, one impossible sequence. Recorded as the concrete argument for the causal-predecessor part of the Wave 2 declaration: the events have an order, and checking each edge against a date is not the same as checking the path.
- 2026-07-31 — **STRUCTURAL: ten rounds, ~46 findings, and the root cause is that "a published fact is a promise the documents keep" is enforced by three hand-maintained lists that nothing reconciles.** This is the entry worth reading if you read one. The money layer asserts one invariant, and a money fact has to appear in three places for it to hold: `money_manifest_block` publishes it, a fact-aware template renders it, and `_validate_money` checks it. Three enumerations bind those sites — `GOVERNED_MONEY_FIELDS` (fields), the fact-aware registry in `build_fact_aware_templates` (subtypes), and the rule bodies in `_validate_money` (checks) — and **nothing derives any of them from the others, so nothing fails when a fact is in one list and not another.** Sort the findings by that lens and most of them stop being separate bugs:
  - *Published, not rendered*: ISC-177 (six wage/rate fields on no page), ISC-201/ISC-206 (the settlement's three dates), ISC-174 (counts without their records).
  - *Rendered, not governed*: ISC-211 (the stipulated award computing its own money — a document rendering money no list knew about), ISC-213 (a carrier rendering somebody else's money).
  - *Governed one way only*: ISC-200 (settlement skipped entirely), ISC-207 (extras refused, absences accepted), ISC-217 (`basisSource` published and never checked).
  The layer keeps being *correct once someone looks* and keeps having another place nobody looked. That is the signature of enumeration, not of a large surface. Size explains a long list of findings; it does not explain **five consecutive rounds each finding a defect inside its predecessor's fix**, which is a property of a system where fixing one site cannot tell you about the other two.
- 2026-07-31 — **STRUCTURAL, the fourth failure class, named by the reviewer and conceded: *governed but semantically misbound*.** The first three classes are about a fact being missing from one of the three sites. This one is about a fact being *present at every site and still wrong*: the carrier holds the expected token, and the token is malformed (ISC-220's `$1,001.23.20`), or labels a different predicate (ISC-221's gross doubling as the PD component), or satisfies its local date relation while violating the event before it (ISC-222's order preceding its own instrument). All three passed a carrier-plus-substring sweep. The consequence for the Wave 2 declaration is that field → carrier → date-relation is not enough; it also needs a **stable semantic definition**, an **evidence parser rather than a substring locator**, and **causal predecessor relations between events**. Recorded because it changes the shape of the proposed refactor, not just its size.
- 2026-07-31 — **STRUCTURAL, corollary: time is the second axis and it is enforced per-site too.** ISC-175 established "no document is dated before what it prints" for payment records. The same rule was then violated and re-fixed four more times — ISC-206 (the release), ISC-214 (payment records again), ISC-215 (the order dated after its own approval), and round 4's original settlement placement. One rule, five discoveries, because it lives in prose and in whichever check happened to be written rather than in the table that says which document carries which fact.
- 2026-07-31 — **STRUCTURAL, the proposed Wave 2 change, which is cheaper than the next six rounds.** Make the governance table carry, per field, *the document that renders it and the date relation that document must satisfy* — roughly `("settlement.approvalDate", ORDER_APPROVING_SETTLEMENT, "on")`, `("settlement.fundingDate", BENEFIT_PAYMENT_LEDGER, "on-or-after")`, `("wage.averageWeeklyWage", WAGE_STATEMENTS_*, "any")` — and generate the render sweep, the document floor, the carrier rule and the validator's presence check from that one declaration. Then *published-but-unrendered*, *rendered-but-ungoverned* and *carried-by-a-document-that-predates-the-event* become unrepresentable rather than findable. A second, narrower piece: there is no enumeration of **substrate templates that compute money**, which is why ISC-211 sat undiscovered through five rounds — the C&R was covered because someone noticed it, and the stipulated award was not because nobody did. The scan must inventory **currency formatting and hardcoded arithmetic**, not only random draws — the stipulated award's `* 0.67` temporary-disability rate was neither a draw nor a constant, and it was wrong. The precedent for the fix already exists in this package: `substrate_organization_pools()` reads `wc_constants.py` **live** so a pool that grows upstream is swept without anyone editing a fixture. The money equivalent — a live scan of substrate templates for monetary draws, with a test that each is fact-aware or explicitly waived — closes the class instead of the instance. Recorded here rather than built now because it is a refactor of the layer's spine and this PR is already six review rounds deep; doing it under a fresh ticket with the corpus stable is the right sequencing, and the four sibling tickets attach to the *schema*, which this would not change.
- 2026-07-31 — **Round 5 overturned round 4's headline fix, and the shape of the error is worth keeping.** Round 4 was right that three published dates appeared on no page, and wrong about the remedy: it put them on the nearest document that mentioned settlements rather than on the document that effects each event. The corrected rule is one sentence — *a published date is printed by a document dated on or after it* — and it is the ISC-175 payment-record rule stated generally. Rounds 2, 3, 4 and 5 have each found a defect the previous round's fix created; four for four is no longer a coincidence, it is evidence that a remediation deserves the same scrutiny as the code it remedies.
- 2026-07-31 — **A claim in this ISA was false and is corrected in place rather than quietly.** ISC-200 recorded "a settlement missing a governed field refused, with a passing control". The governance loop never checked for missing fields at all; the probe behind that claim tested extras and shape. ISC-207 restates it with the check that now exists. Recorded because the standing rule here is to scope a claim to what was actually verified, and this one was not.
- 2026-07-31 — **A survivor is a question about the sweep before it is a question about the guard.** Disabling the floor's approval carrier changed nothing, which looked like a redundant guard of the kind round 1 deleted. It was not: for a *derived* settlement the walk dates its own order on the approval day, so the two coincide by construction and the sweep only ever exercised that case. An authored approval date separates them by up to three years. The rule that fell out: when a mutant survives, ask first whether the probe reaches the branch, and only then whether the branch is needed.
- 2026-07-30 — **Round 4: the group the review had spent the most findings on was the group nothing checked.** Rounds 2 and 3 both landed settlement work — whole-dollar grosses, paired dates, the unfunded state, the anchor bound — and `_validate_money` had been skipping `settlement` in its governance loop since the key existed, because "absent for a case that did not settle" was written as `continue` on the key rather than on a `None`. Every fix went into the one group whose output was never read back. The general lesson: an exemption written for a *value* condition and coded as a *key* condition survives every later change to that key, and nothing about the exemption looks wrong when you read it.
- 2026-07-30 — **Round 3's tagged-union finding was withdrawn by the reviewer in round 4**, on the argument recorded at the 07-30 REFUTED entry below: `kind` is already the discriminator and the Wave 2 extension is additive. Recorded because a refutation that survives a re-review is worth more to the Wave 2 author than one that was merely asserted.
- 2026-07-30 — **A published field with no page behind it is the defect this layer exists to remove, and it was in the layer.** `approvalDate`/`fundingDate`/`fundingLagDays` were ground truth an analyzer would be scored on, printed on no document in a benefit-free settled case. The rule now has two halves that must both hold: the C&R renders its own terms, and `validate --out` refuses the dates when no document in the folder could carry them. One half alone is a promise; two halves is a check.
- 2026-07-30 — **A probe another rule can satisfy is not checking the rule it names.** Re-running the round-2 campaign on the round-4 tree found a mutant surviving: ISC-201's new message also contains the words "payment record", so the missing-record probe passed on a different rule's output with its own guard disabled. The probe now asserts on "benefit event". This is the second time a re-run of an *older* campaign has found something (the first being ISC-203 rewriting a line round 3 mutated), which is the argument for re-running every campaign on the final tree rather than trusting the round they were written in.
- 2026-07-30 — **Round 3 found the weak probe in round 2's own guard.** The governed-field render sweep matched published values anywhere on the page, and `methodSource` and `patternSource` are both usually `derived` — so deleting one row left the other supplying its text and the sweep stayed green. It now asserts the value after its own label. Worth recording because the guard was itself a remediation: a probe written to catch a class of defect can carry an instance of that same class.
- 2026-07-30 — **Round 2 found a defect the round-1 fix created, and that is the argument for reviewing remediations.** Forbidding the shape knobs beside a listed history closed a real label hole (a steady history shippable as `pattern: irregular`) and opened the mirror of it: the author could no longer state a pattern, so the field default published `regular` over a history whose coefficient of variation was 0.9500. The lesson is not that the first fix was wrong; it is that a validator which removes an input has to say what now supplies it. `pattern` is derived and sourced, exactly as `method` already was.
- 2026-07-30 — **Round 2 finding REFUTED: "`SettlementFact` must become a tagged union before Wave 2."** `kind` is already the discriminator, and the extension Wave 2 needs is additive: a stipulated award object attaches beside `gross_amount` without renaming a published field, which is the only change class this schema is trying to avoid. A stipulated award also *has* a total value, so `grossAmount` is not meaningless on that branch — it is the figure the file settles at. Splitting the model now would be modelling AJC-45/46's disbursement from inside Wave 1, which is the scope line this ticket already drew. Recorded so the Wave 2 author inherits the argument rather than the conclusion.
- 2026-07-30 — **The horizon is the timeline's, not the benefit window's.** `_derive_benefits` already bounded advances by the *benefit* window (resolution or application date). That is the right window for scheduling and the wrong one for the paid date: a payment is dated by when it cleared, and the document that reports it is clamped to the timeline horizon. The two only diverge under a long delay, which is why the schedule check looked sufficient for a whole round.
- 2026-07-30 — **Review finding REFUTED: "the anti-criterion is false because a wage-free C&R still renders random monetary terms."** True as an observation, wrong as a finding. The substrate's compromise and release has always invented a gross, a fee, costs and a set-aside; that is `origin/main`'s behaviour and this ticket's own scope note leaves disbursement to AJC-46. The anti-criterion is about *this layer's* artifacts, and it is evidenced by byte identity: regenerated against an `origin/main` baseline, all 331 rendered documents are byte-identical and the only changed lines in the 8 manifests are the `generator` version string. A layer that changes no byte cannot have added a money artifact. Wording sharpened from "zero money artifacts" to "zero artifacts of this layer, and no byte moved" so the claim states what the evidence proves. The underlying observation — the corpus contains unlabelled money on the release — is real and is recorded for AJC-46, which owns the disbursement.
- 2026-07-30 — **Review finding PARTLY REFUTED: "`rate_basis_for` must fail closed in both directions to meet KB-167."** The forward half is unreachable: a date of injury after the anchor is already refused by `CaseSeed`'s runway validation (probed at 2027-01-01 and 2030-01-01, both rejected), so the "2099 gets 2023 rates" case cannot arise from a seed — only from calling the seam directly. The backward fallback is a deliberate call and stays: a synthetic corpus should not become un-generatable because a seed reached back further than a placeholder table does. The divergence from KB-167 (17 eras, fails closed both ways, forward bound because its SAWW snapshot expires each January 1) is real and is now **the recorded integration contract** rather than an unexamined default: whoever lands KB-167 replaces this function's body and must decide then whether out-of-range raises, with the seed-level runway rule as the reason it has never had to.
- 2026-07-30 — **Review finding ACCEPTED on a technicality worth naming: "mutually redundant" was a sound characterization, but the campaign that produced it was aimed too narrowly.** The cross-model reviewer reproduced the `money_manifest_block`/`_dollars` pair and agreed the redundancy is genuine. It also found what the 17-mutant campaign had missed — `compute_aww` and `select_method` were live, exported, and unpinned. The lesson is not that the pair was excused; it is that a mutation campaign enumerated by hand covers what somebody thought of. The replacement guard enumerates `money.__all__`, so a new public function is covered the day it is exported. One redundant guard was also *removed* rather than defended, on the same principle: a guard no probe can reach is an unverified claim that reads like one.
- 2026-07-30 — **`examples/money-showcase.yaml` rather than money in the demo caseload.** Adding wages to a demo seed would move the demo's bytes and cost the clean 353/0 anti-criterion evidence, which is the single strongest piece of proof this ticket has. The showcase carries the seven exercising cases instead — the same call Phase 3b made for `personas-showcase.yaml`, and the same unearned-zero note applies: the demo proves the layer is inert, the showcase proves it works.

## Changelog

- 2026-08-11 — **AJC-61 (M2, the assertion layer):** `scenario.medical_assertions` opens the
  second medical-story ledger — contentions / medical opinions / apportionment assertions
  graded `supported|thin|unsupportable` against the frozen Escobedo v2 rubric, with the
  polarity rule as the contract (divergence from world truth is case content; only internal
  incoherence fails, with byte-exact templates). Labels and a redacted world-truth projection
  export ONLY to the truth manifest's `assertions` channel (envelope unchanged at `1.0.0`,
  `channelVersion 1.0.0`, canonical `ledgerDigest`; `validate --out` re-validates and
  re-grades substrate-checkout-free). Hikida rides typed fields, not a fifteenth
  `DoctrineHook` (AJC-62 owns the enum member and its showcase re-record). The 15-family
  `medical-assertions:` sampler is semantically keyed behind the gate; the empirical
  constants are measured over the frozen 6,000-case cohort and pinned, never guessed; the
  label-position leakage anti-probe (reserved keys + bare `unsupportable`, DOCX `docProps`
  and PDF metadata included) holds the scorer boundary with positive controls. Anti-criterion
  held throughout: all four golden corpora byte-identical with the block absent.
  **Fix round 1** (sol review PR #44; counsel ruling AJC-61/D1, 2026-08-11): condition
  candidacy reverted to world-truth type (visibility is the B.5 evidence predicate only) and
  the firefighter evidence override removed; suppression re-keyed on the post-shaping
  surface with every ID assigned in one final labelling pass; opinions given their own
  foundation recipe with `OPINION_RECIPE_WEIGHTS` solved against the measured worst-of drag
  (opinions 0.7704 / assertions 0.7900 supported, in band); the leakage probe extended to
  parsed docProps property names, the raw PDF Info dictionary, OCR-only pages and decoded
  EML with a planted control per position and detector mutants m19-20/21; and the
  re-measured contention supported share — 0.5880 — sits below the ruling's 0.65 floor,
  pinned exactly and deliberately left RED as an open counsel question.
  **Fix round 2** (sol): XMP element/attribute NAMES and RAW annotation xref keys enter the
  reserved-key scan (`.info` cannot see a custom key — live-probe proven; controls + mutants
  m19-23/24); endorsement grades key on the unique pending reference, never the shareable
  suppression tuple (m19-22, reproduced RED on cohort-4803 first); post-type draws salt on
  the full Part-3 stream key — family + rule-6 surface + normalized grounding IDs — with the
  pre-type reading documented and flagged for spec ratification (m19-25; ratified verbatim
  in sol round 3). Post-F3 cohort: contentions 0.5857, opinions 0.7698 / assertions 0.7900
  in band, every pre-type pin byte-identical. **Counsel ruling AJC-61/D1-R2 Option A2
  (2026-08-11)** closed the floor question: 0.5857 is the modeled behavior, the contention
  floor is 0.55 with the exact pin retained, opinions/assertions stay 0.75–0.85, psych rules
  and the 2:1 residual unchanged — the designed RED no longer exists.
- **conjectured:** same-machine double-run md5 comparison proves generation determinism (Phase B gate design).
  **refuted by:** advisor-demanded clock-shift probe — TZ=Australia/Sydney drifted 55-60 of 289 files (EML Date offsets, PDF/docx timestamp normalization, substrate date.today() content, Faker clock-relative date_of_birth); the CLI-level gate also masked an in-process leak the two-zone test caught.
  **learned:** determinism gates must vary the environment axes the guarantee spans — process, hash seed, timezone, entrypoint — not just repeat the run; a passing gate that shares the leak's precondition proves nothing about the class.
  **criterion now:** ISC-12 evidence requires cross-process AND cross-timezone md5 identity (test_timezone_determinism.py + CI-runnable probes), and every new output format must add a container-timestamp normalization with its own test.
- **conjectured:** a "full-range render test" (ISC-54 as one probe) covers the per-subtype render surface, justifying the ISC-floor waiver.
  **refuted by:** Cato audit — the probe ran over the 78 subtypes the demo emits (22.1% of 353); 275 subtypes have never been rendered; no automated no-fallback assertion exists.
  **learned:** a scope-limiting qualifier ("full-range" = full range of the demo) reads as total coverage to both executor and same-family reviewers; the waiver reasoning rationalized away exactly the probe that would have caught it.
  **criterion now:** ISC-54 states the all-353 scope and is evidenced by `test_render_coverage.py`; template provenance and a fallback flag reach every manifest entry; `validate --out` refuses a fallback by default. **Resolved 2026-07-27 late** — the forced render found exactly 3 fall-throughs (the engine's own overlay subtypes) and they were fixed at dispatch rather than recorded as exceptions.
- **conjectured:** `zeroRealPii: true` and "generation writes nothing outside `--out`" are properties the generator can assert about itself.
  **refuted by:** Cato audit + this round's probes — the PII flag was a hardcoded literal that no input could falsify, and the write check watched `git status` on a repository working tree, which is the one place a stray temp file, font cache or dotfile would never land. Sweeping the *output* then found the leak both had missed: the substrate draws carrier and defense-firm names from pools of real California companies, so every seed that did not name its own carrier shipped a real organization on a fabricated claim file.
  **learned:** an anti-criterion has to be measured on the artifact, not asserted about the process that made it — and it must be able to fail, which means building the input that makes it fail and keeping that as a test.
  **criterion now:** ISC-73 is a denylist sweep over extracted document text plus every manifest cast field, with a positive control; `zeroRealPii` is computed from `CaseCast.provenance` and published with its derivation; ISC-74 diffs a sentinel `HOME`/`TMPDIR`/XDG tree around a subprocess generate.
- 2026-08-02 — **ISC-148, cross-model review round:**
  **conjectured:** the ISC-148 fix stops `_shape_discovery` outranking the document controls, evidenced by a 15-test class that exercises `exclude`, `include_only` and per-subtype overrides.
  **refuted by:** a GPT-family reviewer, by execution — `{type: DISCOVERY, max: 0}` still emptied the file and still blamed the lifecycle stage, on a seed already at `discovery`. `documents.overrides` has two spellings and the fix read one of them. `global_cap` reproduced the same wrong answer through a second path: the warning reached for the stage sentence whenever no per-subtype control matched, without consulting `stage_is_a_cause`. The trim side was never touched and dropped an explicitly-counted subtype to zero, silently.
  **learned:** the guard set was built from the same reading of the control that the fix was, so the two agreed with each other and neither touched the code. Coverage counted in tests said 15; coverage counted in *ways the seed can spell the control* said one of two. A fix aimed at "X outranks control C" has to enumerate C's spellings before it can claim anything, and the enumeration belongs in the probe, not in the prose. The tick on the criterion was the visible symptom — it was written from the fix's own scope.
  **criterion now:** ISC-148 evidence enumerates both `documents.overrides` spellings and both directions (refill and trim), distinguishes a pin from a ceiling with the measured ladder that separates them, and carries the opposite-draw pair `m12-8`/`m12-10` so that over-protecting the trim fails as loudly as under-protecting it. 23 tests, mutants `m12-1..m12-10`.
- 2026-08-02 — **The mutation gate:**
  **conjectured:** the gate proves every registered fix, because a non-zero pytest exit from the named guard means the guard failed.
  **refuted by:** a preflight over the registry — 10 of 112 node ids named test classes an earlier round had renamed, and `m2-2` named a class that never existed at any commit. pytest collected nothing and exited **4**, which the gate scored identically to a failing assertion. Separately, the gate's own probe scored 7 spurious `SURVIVED` in 10 runs: CPython invalidates a `.pyc` on *(mtime, size)*, and a same-length mutation applied inside one mtime tick is imported from cache un-mutated.
  **learned:** a scorer that reads exit status alone cannot tell "the guard failed" from "no guard ran", and a mutation the interpreter never loads proves nothing in either direction. Neither failure is visible in the gate's own output — which is the argument for the scorer carrying its own suite, at the same standard it enforces.
  **criterion now:** RED is a five-clause conjunction (guard green on pristine source; mutated source compiles; the node collects exactly once; setup and teardown pass with the failure in the call phase; the call-phase exception is an assertion). `--preflight` runs per shard in CI ahead of that shard's mutants. `PYTHONDONTWRITEBYTECODE=1` rides into every subprocess. `tests/test_mutation_gate.py` and the nine `g-*` mutants hold the scorer to its own rule.
  **refuted again, same round:** clause 1 was not enforced. `pristine_verdict` read `setup` and `call` and never `teardown`, while `_phase_verdict` rejected a failed teardown on the mutated side — the two halves of one comparison disagreeing about what "green" means. A guard whose fixture teardown fails is `1 passed, 1 error`, exit 1, red in CI, and was cached GREEN; its mutant then scored RED and counted as evidence. The reviewer also observed that **no registered mutant touched the pristine-green predicate at all**, so the clause was unguarded as well as wrong.
  **learned:** writing the conjunction down is not enforcing it. Every clause of a claim like this needs its own mutant, or the clause is prose. The one clause with no `g-*` behind it is precisely the one that was broken, which is not a coincidence — it is what an unguarded clause looks like from the outside.
  **criterion now (amended):** `teardown` is compared against passed-or-absent on the pristine side too, `g-9` mutates that clause, and `test_a_guard_with_no_teardown_phase_is_still_green` pins the opposite draw so the default cannot silently accept everything.
- 2026-07-28 — **ISC-21 KB-grounding follow-up (iteration 2, commit e1d1b62):**
  **conjectured:** the hooks' short markers ("Benson", "Guzman", "Stevens", …) are the right grep key for both the positive content-depth probe and the hook-free anti-probe.
  **refuted by:** first test run — generated casts draw surnames from the same civic pools, so a hook-free case can legitimately contain "Stevens" as an applicant or physician name; the anti-probe would fail on coincidence, not regression. Separately, the first cross-case comparison measured the pre-existing ≥2-hooks complexity flip, not the injection.
  **learned:** a negative probe must key on strings only the code under test can emit (full citations and the injected section headings), and a content-injection probe must isolate at the render level before asserting at the case level, or every upstream coupling becomes part of the measurement.
  **criterion now:** ISC-21.5 keys on citations + injected headings with a code-path assertion; ISC-21.4 probes `render_document` directly (14 hooks × 2 registers) with the plan-level wiring asserted separately; the complexity coupling is pinned by its own named test.

## Verification

Evidence is grouped; every probe was run in the main checkout on the fast-forwarded branch (commits f9e4ed7, 170fe9e, dac1d8a) unless noted.

- ISC-1..10: Bash — `wc-caseload --help` lists all four commands; venv install clean; root README/CLAUDE rows + ci.yml paths-filter and quality-gate job present (rg probe); docs quartet on disk with GBS footers.
- ISC-11..20, 22..29: pytest — 225-test suite covers schema, validation errors, deep-merge, auto-derivation determinism, distribution presets, full control-precedence matrix; `seed --template` round-trips through the loader.
- ISC-12/69: Bash — demo caseload double-run: 289 files md5-identical; cross-process and cross-TZ (`TZ=Australia/Sydney`) runs also identical (initially FAILED with 55-60 drifting files; fixed in dac1d8a; re-probed clean). Entrypoint parity added: `python -m wc_caseload_engine`, `-m wc_caseload_engine.cli` and the `wc-caseload` console script write byte-identical trees (test_entrypoint_parity). **Second review round:** the sentinel bypass reproduced first — `WC_CASELOAD_HASH_PINNED=1` with `PYTHONHASHSEED=1` vs `=2` gave tree md5 `86f0a84…` vs `62ccd53…` (3 files drifted: 2 PDFs + the manifest) — and is now identical at `4cf9286…` with zero per-file drift. Three subprocess regressions in `TestHashSeedGuard`: the sentinel buys no bypass, a sentinel already at `MAX_REEXEC_HOPS` exits non-zero naming both `PYTHONHASHSEED` and the guard variable instead of exec-looping, and one hop still suffices on the ordinary path. Demo gate re-run after all three fixes: 346 files, tree md5 `8af3070…` identical across a double-run and a `TZ=Australia/Sydney` third run, zero per-file drift; `validate --out` OK at 7 manifests / 331 documents / **0 fallbacks**.
- ISC-27: chi-square over a 120-document case against a 0.5/0.25/0.15/0.10 seeded mix, α = 0.001; positive control rejects an all-pdf draw; the shipped demo's 331 documents pass the same test against its own 0.6/0.25/0.1/0.05 mix (test_format_mix).
- ISC-30..38: pytest test_lifecycle_paths + test_date_spine; live manifest probes: denial path docs present; recon windows 23d/18d (≤25) and 42d/49d (≤60). **ISC-32/33, second review round:** the boundary collapse reproduced first — 84 of 90 boundary-valid seeds (30 rng draws × qme / ur / ur+imr, injury exactly on each branch's floor) produced a non-increasing chain, most of them every document on 2026-01-01 — and is now 0 of 90. `TestBranchChainsAreFittedNotClamped` asserts strict ordering on the QME, UR-upheld, UR-overturned and UR+IMR chains at their floors, each as a single seed and as a 30-seed property test, because one passing draw of an `rng.randint` chain is an anecdote.
- ISC-39..46: live probe — nguyen case: 3 lien tracks (medical/hospital/pharmacy), 3× LIEN_RESOLUTION agreements, conference + lien pretrial statement docs, 17/17 distinct lien dates after the ordering fix.
- ISC-47..53: live probes above + tests per recon outcome (denied/granted_remand×settled/further_litigation).
- ISC-54..59: test_render_coverage — **all 353 canonical subtypes forced through `render_document`: 0 raised, 0 `GenericDocumentTemplate` resolutions, 0 fallbacks, 0 under 500 B, 353/353 PDFs parse with ≥1 page; `KNOWN_UNRENDERABLE_SUBTYPES` is empty** (~25 s). test_rendering (four formats open in native readers). ISC-58 by full-case sweep: 40/40 text-bearing documents of `alvarez-denied-recon-remand` name the applicant; every post-filing captioned document carries the ADJ number; 0 intrusions from the other six casts (test_coherence). **Second review round:** employer-trio coherence added to the same file — the reproduction (`rng_seed=2` + `industry: healthcare`) gave "Ashvale Construction Group" / healthcare / "Laborer" and now gives "Ashvale Care Network" / healthcare / "Medical Assistant"; asserted across four industries × ten draws, plus a seeded `applicant.occupation` still outranking the industry and a free-text industry degrading without raising.
- ISC-60..64: Bash — `taxonomy-check` exit 0 at 353/353 parity; `validate --out` OK (276 docs canonical, checksums match); corpus filename regex 0 failures on 50 PDFs; neutral naming is default.
- ISC-65..68: manifest probes — per-doc fields incl. md5/fileSize/mimeType; provenance {zeroRealPii: true, substrateSha, seedHash}; caseload manifest carries stage/resolution/lien/recon summaries + subtypeCoverage {78, 353, 22.1%}.
- ISC-70..74: 307 tests; ruff clean; anti-probes — no substrate file copied (content-hash test + Forge trace); ISC-73 denylist sweep clean over 256 text-bearing demo documents and all 7 manifests' cast fields, positive control fires, 400 engine-coined organization names checked against the list; `zeroRealPii` computed and provably falsifiable; ISC-74 sentinel `HOME`/`TMPDIR`/XDG tree shows 0 created, 0 modified, 0 removed files after a subprocess generate (test_anti_probes).
- ISC-75..82, 84: perspective build (commit 217b6d2) — 257 tests (+32) incl. mirrored-facts invariant (identical cast/ADJ/dates across perspectives), work-product swap, applicant-only absence + forced-override WARN path, comparative frequency probe (defense CLAIMS_ADMINISTRATION=9, INVESTIGATION=3 vs applicant baseline), 7-case demo double-run + TZ run MD5-identical (346 files), validate OK (331 docs), six pre-existing cases byte-stable (only intended manifest deltas: perspective field + seedHash).
- ISC-83: README/CHANGELOG + HTML user guide Section 4 (Case perspective) — guide live-verified against perspective.py and a 7-case run.
- ISC-21: CLOSED at iteration 2 (commit e1d1b62) — doctrine_hooks now inject per-hook content flags into the plan AND per-hook language into rendered documents. Evidence below.
- ISC-21.1/21.2: pytest — table-completeness suite over all 14 `DoctrineHook` values (marker, citation, 3 medical + 3 legal paragraphs each, every paragraph contains its marker); all 36 distinct target subtypes asserted members of the 353-key canonical taxonomy (test_doctrine_content.py, part of 462-test green run).
- ISC-21.3: pytest — per hook, `build_case_plan` with a forced target-subtype count override yields ≥1 `PlannedDocument` carrying the hook in `content_flags`; planner→renderer wiring covered by a full `generate_case` assertion.
- ISC-21.4: pytest — 28 direct `render_document` probes (14 hooks × medical/legal registers), pypdf-extracted text contains each hook's marker. Live spot-probe on the demo: castellanos (ogilvie+kite) PDF text quotes "Athens Administrators v. WCAB (Kite) (2013) 78 Cal.Comp.Cases 213" and "Ogilvie v. WCAB (2011) 197 Cal.App.4th 1262".
- ISC-21.5: pytest negative probe keys on citations + injected headings (surname markers collide with generated casts — see Changelog); code-path assertion shows empty `content_flags` uses the registry class unwrapped. Live probe: whitfield-early-intake (hook-free) is byte-identical to the pre-injection baseline and its extracted text has zero occurrences of either injected heading; the same headings appear 13× across the tree, all inside the 6 hook-bearing cases.
- ISC-21.6: manifest pytest — per-doc `contentFlags` present iff non-empty; determinism: post-injection demo double-run + `TZ=Australia/Sydney` third run all tree-md5 `7ffc5050…` (vs pre-injection baseline `8af30704…`), 346 files, only 24 files across the 6 hook-bearing cases changed, file count delta zero, `validate --out` OK at 7 manifests / 331 documents / 0 fallbacks. Full suite 462 passed (110 new), ruff clean, substrate untouched.
- Remediation round (2e35cc9): 501 passed, ruff clean; demo double-run + TZ third run tree-md5 `c837d9df…` identical ×3; `validate --out` OK, 0 fallbacks, the two designed incoherent-hook warnings surface with actionable text (nguyen/benson, ramirez/gfpa) and are informational, not blocking; rem-vs-prior diff fully attributed — 8 files: 7 manifests gained `plannedDocumentCount` alongside emitted `documentCount` (whitfield's pure schema addition, `0/0`), 1 document changed content (nguyen APPORTIONMENT_REPORT, the reworded contention-framed Benson passage); `zeroRealPii` remains `true` in all 8 occurrences; top-level caseload manifest byte-identical.
- Iteration 3 / Phase 1 (d98be53, db944f8): ISC-85..89, 91..95, 97..100 MET; ISC-90/96 met as refined (see Decisions). Key probes: double-derivation model-dump identity with and without a `scenario` block; 4 bad-seed rejections with actionable errors; manifest `caseFacts` block + `case_facts.yaml` round-trip; dispatch assertions (unregistered → substrate class, registered → subclass, registry inert without a ledger); absent-EMG nowhere in the QME with performed-EMG positive control; no-surgery case carries zero surgical language with positive control; one CPT named identically in operative record, QME, and PR2; >1 distinct provider across subpoena packets; 32-test coherence harness, every rule with a red-proven planted control; full-demo byte diff vs v0.2.0: 303/331 identical, 28 changed all traced (23 registry + 5 ISC-94), 0 unexplained; demo determinism tree digest `603fd9f6…` identical ×3 incl. TZ=Australia/Sydney; `--version` 0.3.0, 5 numbered compatibility notices; 587 passed, ruff clean, substrate diff 0 files.
- Iteration 3 / Phase 2 (40d4024 + remediation 539b2a9, liveness a2b6390, CLI-guard 3245028; merged as PR #24 dde9cbe): ISC-92.1 and 101..116 all MET. Key probes: never_treated planner suppression with lien auto-fill filtering (pool-exhaustion fallback attacked and held); gap in visit series, discharge date fencing; monotone trajectory phrases with substrate-pool pin; denied_by_ur requires explicit upheld (substrate rng.choice pin proves the None case could contradict); stated-surgery emission floor + validator biconditional (derived path untouched); EXAMINED REGION per-study attribution; governed QME history; modality audit (34 rows, per-row liveness purged 5 vacuous → 29); actionable-message follows-the-edit meta-guard incl. static CLI-invocation check (flags too); liveness guards over ten hand-written lists (nothing dead — regression guard). Review: round-1 FAIL with three half-built-guard MAJORs, all proven by execution and closed at mechanism level; re-review re-ran convicting seeds + adversarial variants → pass-with-concerns. Gates: 715 passed, byte accounting vs v0.3.0 = 307/331 identical, 24 changed all traced, 0 unexplained; determinism ×3; demo warnings 2; substrate 0 files. v0.4.0.
- Iteration 3 / Phase 3a (ce950e3): adjuster persona + penalty re-gating + structural carry-ins. ISC-117/118/120/122/127/128/130/131/132/133/134 MET; 119/121/123/124/125/126(schema-only)/129 split to Phase 3b — attorney cadence effects, letter-type coherence, discovery unification, message registry. Nothing false published: unimplemented effects are excluded from `caseFacts` and CHANGELOG-listed as "Not yet honoured". Key probes: LC 5814 gated on ledger-recorded lateness — 20/20 stated-negligent plead (each with a recorded late event), 0/20 stated-attentive, 19/100 derived; substrate flat-coin suppression proven by toggle (100 seeds: 3 walk emissions unsuppressed, 0 suppressed) because the demo's 0-penalty result was rejected as evidence; diagnostic_report.py:69 governed when a performed study exists — the starved-ledger path was the real gap and is fixed (ISC-128 answer). Gates: 736 passed, ruff clean, determinism ×3, demo 331/0-changed vs v0.4.0, demo warnings 2, substrate 0. v0.5.0. KNOWN: suite runtime regressed 155s→570s, cause unisolated — first Phase 3b item.
- Iteration 3 / Phase 3a remediation (9dc0d6a; merged as PR #25 d34c744): five review findings closed red-first (controls routing with loud suppression, docstring truth sweep, honest test name, single ledger derivation, diligence unpublished, strict petition-date invariant). Gates: 741 passed in 146.63s — **the 570s regression was the doubled derivation, closed** — ruff clean, byte diff vs v0.4.0 = 331/331 identical, determinism ×3 incl. TZ=Australia/Sydney, demo warnings exactly 2 (nguyen/benson, ramirez/gfpa), substrate 0 files. Confirmation review re-proved each closure by independent execution (741 @ 117.6s vs 245s at the parent commit in the same environment; 120-seed dating sweep 0 violations; include_only membership test). Residuals → ISC-135/136/137.
- Iteration 3 / Phase 3c (ce5d215..451fa80, incl. origin/main merge 34f0eaa): F1/F2 + ISC-125/126/129 ALL MET — Phase 3 scope complete at 147/147. Key probes: F1 red-capability (old probe blind at 57→57, new catches 74→78); F2 404-letter sweep (82% at 5d, 0 unanchored, per-case derived bound); ISC-125 PDF-extracted anchor dates vs manifest (6 event-driven, 0 control); ISC-126 four packets ledger=ToC=paper exact (e.g. 5+1+3+4 vs TOTAL PAGES 13), F1 probes red at WIP → green at completion; ISC-129 57 scanned/11 actionable/0 unresolved, six mutation classes red, registry self-caught its author's docstring. Gates: **842 passed + 2 skipped in 192.23s**, ruff clean, byte accounting v0.6.0→0.7.0 = 331→331, 325 identical / 6 changed / 0 unexplained (5 SUBPOENAED_RECORDS_MEDICAL + 1 EMPLOYMENT, all nguyen-cr-three-liens, = ISC-126 — the only packet-reaching demo case), determinism ×3 digest 42fa4d6d (353 files, incl. TZ=Australia/Sydney), demo warnings exactly 2, personas-showcase post-merge 391 docs / 0 fallbacks / 0 warnings, v0.7.0 with 5 numbered notices, substrate 0 files. Docs: README "Accepted but not yet rendered" section was FALSE after ISC-126 and replaced ("The discovery block"); user-guide gained the six-row scenario field reference it never had; CLAUDE.md module map + test table updated, stale 741→844. Implementer self-correction logged: a claimed `caseFacts.packetPages` publication checked against the generated manifest, found absent, README corrected to say the field is deliberately unpublished (the packet states its number on its own cover sheet).
- Iteration 3 / Phase 3b (d82a1be..f0a491e): ISC-119/121/123/124/135/136/137 MET; 125/126/129 split to Phase 3c. Key probes: delay chain 0/0/3 demand letters across attentive/ordinary/negligent with ledger parity; letter ordinals [0,0,0,0]→[0,1,2] after the `_SpecCapture` fix, four letters render three distinct types with liability accepted exactly once; cadence gaps on one seed — every_30_days 30×5, event_driven 4/31/94/98/162 (each 1–5 days behind a real document), sporadic 24/120/41/96/33 with the >90-day gap; meta-guard self-fire message captured ("cadence … moved 35 document(s)"); key-walk planted-leak control catches all five banned names. Gates: 786 passed exit 0 (one real failure surfaced by the suite fixed in f0a491e), ruff clean, byte accounting vs v0.5.0 = 331→331, 310 identical / 21 changed / 0 unexplained (19 ADJUSTER_LETTER_INFORMATIONAL + 1 ADJUSTER_LETTER_REQUEST = ISC-121; 1 TREATING_PHYSICIAN_REPORT_PR2 = the ordinal fix, arithmetic exact: one demo case holds two PR2s, only the second moved), determinism ×3 (first read DIVERGENT — implementer's own baselines predated the version bump, a procedural error self-disclosed; re-run at HEAD identical incl. TZ=Australia/Sydney, tree 66e55a3f…), demo warnings exactly 2, v0.6.0 with 5 numbered notices, substrate 0 files.
- Iteration 4 / money Wave 1 (AJC-43): ISC-151..169 all MET. **Gates:** full suite **965 passed, 2 skipped in 164.91s** (from 849+2 at `origin/main`: +77 money probes, +39 from the thirteen new message-registry entries), ruff clean on `src/` and `tests/`, `merus-test-data-generator` diff 0 tracked files, demo determinism ×3 including `TZ=Australia/Sydney` (353 files identical), demo `validate --out` OK at 7 manifests / 331 documents / 0 fallbacks, demo warnings exactly 2 (the designed nguyen/benson and ramirez/gfpa incoherent-hook pair, unchanged), v0.8.0 with 5 numbered compatibility notices.
  - **Anti-criterion, the headline number:** full demo caseload regenerated at this branch vs `origin/main` (0.7.0) via `git archive` of the baseline tree — **353 files: 345 identical, 8 changed, 0 unexplained.** All 331 rendered documents byte-identical. The eight are the seven case manifests plus the caseload manifest, each a single-line diff on the `generator` provenance string (`@0.7.0` → `@0.8.0`) — verified line by line, not asserted. Measured before the version bump the diff was literally empty (`diff -rq` clean, 0 files), which is the money layer's own cost stated separately from the release's. No committed seed carries `scenario.wages`, so no committed manifest gained a `money` key (`test_the_demo_caseload_publishes_no_money` asserts it over the shared demo fixture).
  - **Anti-criterion, the earned zero:** a wage-free case forced by `include_only` to hold all four money-governed subtypes (`WAGE_STATEMENTS_PRE_INJURY`, `TD_PAYMENT_RECORD_ONGOING`, `PD_PAYMENT_RECORD_ADVANCE`, `COMPROMISE_AND_RELEASE_STANDARD`) carries none of the money markers, no `money` key in manifest or `case_facts.yaml`; positive control on the same probe finds all three markers on a money-bearing case. Code-path half: `_load_template(subtype, fact_aware=True)` returns the engine subclass for those subtypes either way, and the subclass delegates on `money_facts is None` — the guarantee is delegation, not dispatch luck.
  - **Acceptance, arithmetic:** irregular-earnings seed → 26 biweekly periods on the statement, AWW recomputed from the published gross/weeks/in-kind matches the ledger exactly across 7 history shapes; explicit-earnings case pinned to a hand-computed `1000.00` over 4 weeks; comp rate re-run through `compute_comp_rate` reproduces `tdWeeklyRate`, `pdWeeklyRate` and `tdBound` at 4 wage levels. Cap fires at `base_weekly_wage: 6000` (`1539.71`, `tdBound: max`) and does **not** at 1000; floor fires at 50. Four injury dates (2010/2016/2020/2024) yield four distinct basis+rate pairs.
  - **Acceptance, on paper:** wage statement extracted text contains the method name, `averageWeeklyWage`, both rates and `COUNSEL-UNCONFIRMED`, and the first six periods' gross figures; payment record contains `NO BENEFITS PAID`, the 75-day gap, the 33-day lateness and `tdTotal`; C&R contains the ledger's `88,000` gross.
  - **Acceptance, determinism:** money-bearing double render — every document md5 identical, `caseFacts` identical, `case_facts.yaml` byte-identical. Money-showcase (7 cases / 516 documents) double-run and `TZ=Australia/Sydney` run all identical at 538 files; `validate --out` OK, 0 fallbacks. Decimal-context sweep (2/3/5/6/9/28/40/50) identical.
  - **Mutation testing, 17 mutants.** 15 RED on first pass (method-selection order, rate ceiling, rate floor, DOI keying, in-kind, gap knob, diligence→lateness, approval/funding collapse, the money gate itself, wage-table rewrite, settlement pin, floor de-duplication, validator wage-statement rule, `money()` pin, derivation-body pin). **One STILL-GREEN and it was a real finding**: unpinning `money()`'s own decimal context left the whole-derivation probe green because the outer wrap covered it — the guard was widened to probe the public entry points directly and the mutant now goes red. Two further pins (`money_manifest_block`, `_dollars`) survive individually because they are mutually redundant; removing both together goes RED, recorded rather than hidden.
  - **Meta-guards fired on their own, as designed.** The ISC-129 message registry went red on 13 unregistered actionable messages the moment the money validators landed; each is now registered with a trigger seed and its own prescribed edit followed verbatim. Two messages were invisible to the detector (displaced verbs, the ISC-150 hole) and were reworded to registered imperatives rather than widening the vocabulary. ISC-137's `SCENARIO_CLASSES` extended by the six money scenario classes at the moment they existed.
  - **Not claimed:** the money layer is not exercised by the demo caseload (by design — see Decisions), so the demo proves inertness and the showcase proves function; and `earning_capacity` is reachable only by authoring, which the showcase's seventh case and the method-reachability test both witness.
- ISC-241: isolated mutation gate — `1/1 mutants ran their named guard and failed it on an assertion`
- ISC-242: isolated mutation gate — `1/1 mutants ran their named guard and failed it on an assertion`
- ISC-243: isolated mutation gate — `1/1 mutants ran their named guard and failed it on an assertion`
- ISC-244: isolated mutation gate — `1/1 mutants ran their named guard and failed it on an assertion`
- ISC-245: isolated mutation gate — `1/1 mutants ran their named guard and failed it on an assertion`
- ISC-246: mutation shard 3/10 — `28/28 mutants ran their named guard and failed it on an assertion`
- ISC-247: mutation shard 5/10 — `28/28 mutants ran their named guard and failed it on an assertion` (writable temporary mirror used for the registered cross-package substrate mutant; production checkout remained untouched)
- ISC-248: mutation shard 6/10 — `28/28 mutants ran their named guard and failed it on an assertion`
- ISC-249: mutation shard 8/10 — `28/28 mutants ran their named guard and failed it on an assertion`
