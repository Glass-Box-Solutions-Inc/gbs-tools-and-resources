# Agentic Debugger — Environment Variables

# Required
ANTHROPIC_API_KEY="sk-ant-..."

# Optional — Linear ticket tracking (disabled if not set)
LINEAR_API_KEY=""

# Set by workflow at runtime
# CURRENT_FAILURES_FILE="/path/to/current-failures.txt"
# ATTEMPT_NUMBER="0"
