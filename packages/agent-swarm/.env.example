# Agent Swarm — Environment Variables
# Copy to .env and fill in values

# PostgreSQL connection string (required for standalone mode)
DATABASE_URL="postgresql://user:password@localhost:5432/agent_swarm?schema=public"

# CORS origins for WebSocket gateway (comma-separated)
CORS_ORIGINS="http://localhost:5173"
